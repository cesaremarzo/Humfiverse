[build]
  base = "webapp"
  command = "npm ci && npx ng build --base-href /"
  publish = "../docs"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
