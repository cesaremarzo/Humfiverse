{
  "name": "humfiverse-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Testnet demo smart contracts for Humfiverse — NOT audited, NOT for real funds. Mints per-catalogue tokens into a platform-held pool for the fictional demo tracks.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:baseSepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "mint:baseSepolia": "hardhat run scripts/mintCatalogues.js --network baseSepolia"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
