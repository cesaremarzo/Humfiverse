services:
  - type: web
    name: humfiverse-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm ci
    startCommand: node --experimental-sqlite server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.9.0
