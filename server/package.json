{
  "name": "humfiverse-server",
  "version": "0.1.0",
  "private": true,
  "description": "Mock backend for the Humfiverse frontend prototype (libSQL-backed via @libsql/client — a local file by default, a real Turso database in production; see db.js). Only other dependency is ethers, for the Base Sepolia on-chain integration.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "dotenv": "^17.4.2",
    "ethers": "^6.17.0"
  }
}
