{
  "name": "humfiverse-server",
  "version": "0.1.0",
  "private": true,
  "description": "Mock backend for the Humfiverse frontend prototype (SQLite-backed, zero npm dependencies).",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
