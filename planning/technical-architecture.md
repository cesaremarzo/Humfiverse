# Music Royalty Stream Tokenization — Technical Architecture

*Working draft — v0.1, 26 Aug 2026*

## 1. The hard problem, stated plainly

The smart contract that mints and transfers tokens is the easy part — it's a well-understood pattern. The hard part is everything around it: proving a royalty right exists and is unencumbered, getting real-world royalty income into a wallet a smart contract can act on, and doing payouts and compliance (KYC, transfer restrictions, tax reporting) correctly. Any architecture proposal that starts with "which chain should we use" before addressing those is designing the wrong 20%. This doc addresses them in that order.

## 2. System components

**2.1 Rights & royalty data verification (off-chain, but foundational)**
Before anything is tokenized, the platform needs to confirm: (a) who legally owns the royalty stream being tokenized, (b) that it isn't already pledged/encumbered elsewhere, and (c) what its historical and likely future income looks like. This means integrating with — or at minimum ingesting statements from — performance rights organizations (PROs, e.g. ASCAP/BMI/SIAE for Italy), mechanical licensing bodies, and distributor/DSP reporting (Spotify for Artists, Apple Music for Artists, or an aggregator like DistroKid/Believe that already aggregates this). There is no reliable public API that hands over "ground truth" royalty data automatically — every platform in this space (Royal, anotherblock, JKBX) does this verification largely as a manual/contractual diligence process before listing a song, not as a live feed. Plan for this to be a human-in-the-loop onboarding process, at least in v1.

A fourth check belongs alongside those three from day one: **exposure to platform AI-content policy.** As of mid-2026, DSPs are not aligned — TIDAL demonetizes fully AI-generated tracks outright (since 15 July 2026) while Spotify still pays on them normally, focusing enforcement on fraud/bot streams instead (which IFPI estimates cost artists ~$2B/year industry-wide, and which made up roughly 85% of AI-track streams on Deezer). Because a DSP can change this policy unilaterally after a catalogue is already tokenized, onboarding diligence should record, per catalogue: whether any AI tooling was used in the recording and to what degree, and each target DSP's current AI-monetization stance — and the platform should re-check that periodically, not just once at listing. Building this screening in from the pilot, rather than retrofitting it later, is also a plausible differentiator: a diligenced, fraud-screened, authenticity-labeled catalogue is a cleaner asset than the average royalty pool right now.

Note that no DSP has published a bright-line, quantitative threshold for "how much human contribution exempts a track from AI-demonetization" — TIDAL's policy defines only "100%"/"substantially" AI-generated without specifying what "substantially" means, and Spotify's DDEX categories (AI vocals, instrumentation, composition, post-production, lyrics) are a disclosure taxonomy, not a pass/fail test. So this check should be re-run periodically per catalogue, not treated as a one-time pass/fail gate at onboarding — see §2.7 for how the production-financing variant turns this from a pure risk into a documented, evidenced asset.

**2.2 Legal wrapper: the SPV**
A special-purpose vehicle (SPV — e.g., an Italian *società a responsabilità limitata* set up per catalogue, or per song/bundle) holds the actual royalty right, assigned or licensed from the rights holder under a contract. The token represents a claim on the SPV (equity, a revenue-participation note, or a security token depending on the legal path chosen — see legal doc), never the copyright directly. This is the standard structure because "the copyright itself lives on a blockchain" isn't a recognized legal concept in essentially any jurisdiction today — the token is a claim wrapper around a traditional legal object.

**2.3 The royalty collection account ("la cassa"), the transparency covenant, and the oracle problem**
Royalty income is paid by DSPs/PROs into a traditional bank or collection account controlled by the SPV or a royalty administrator (a specialized service like Sentric, Songtrust, or a custom-built collection agreement) — it does not arrive on-chain natively. This account (call it "la cassa") is the single piece of off-chain infrastructure the whole product depends on, and it is identical whether the track is already live on DSPs or is about to be released once the token sale funds production — which is a useful simplification: **the on-chain side of this product is one architecture, not two.** The rights holder should contractually and technically commit, at the moment the token is created (whether that's at catalogue tokenization or at track upload for the pre-production model), to keep that account's statements public/auditable to token holders — a transparency covenant, not just a payment mechanism. In practice this means the royalty administrator publishes periodic statements (or the platform ingests and republishes them) that any token holder can check against what the distribution contract actually paid out.

To distribute what's in the cassa to token holders, the platform needs a bridge from that off-chain cash position to an on-chain distribution event. In practice this means:
- A royalty administrator or the platform's own finance operations confirms receipt of a royalty payment for a given period.
- That confirmation is submitted on-chain, either by a trusted/multisig-controlled oracle role initially, or later by a decentralized oracle network (e.g. Chainlink Proof of Reserve-style attestation) once volume justifies the added complexity and cost.
- On-chain confirmation triggers a **distribution contract** (deliberately not called a "liquidity pool" — see note below) to pull stablecoin (or fiat via an off-ramp) proportionally to token holders, using a standard "dividend/distribution" pattern (pull-based claims rather than a push to every wallet, to avoid gas-cost and failed-transfer issues at scale).

*Terminology note:* what's needed here is a **revenue-distribution vault**, not a DeFi-style "liquidity pool" (an AMM that matches buyers and sellers and prices an asset via a bonding curve). A literal liquidity pool would introduce price-discovery and impermanent-loss dynamics that have nothing to do with paying out royalty shares, and — more importantly — if it lets holders swap/trade their claims against it, it starts to look like operating a trading venue on top of the SPV structure, which is a separate regulated activity (a trading platform/MTF-equivalent under MiCA/MiFID) layered on top of everything already in the legal doc. Keep the distribution mechanism (accumulate the cassa's confirmed income, let holders claim their pro-rata share) and any future secondary-trading mechanism (§3, Phase 3) as two clearly separate systems, decided separately, rather than defaulting to "liquidity pool" terminology that conflates them.

This oracle step is a trust point: v1 will almost certainly be **centralized/attested** (the platform or a named administrator signs off), not trustless. That's an honest tradeoff to state up front rather than promise more decentralization than is achievable at launch — being transparent about it is also a better regulatory posture than overstating "trustless" claims.

**2.4 Token layer**
- **Chain**: an EVM-compatible chain is the pragmatic default given tooling maturity and precedent (anotherblock uses Ethereum + Optimism). A low-fee L2 (Optimism, Base, Polygon) is the right choice for the actual token/distribution contracts, since royalty payouts can be frequent and small-value; mainnet Ethereum can be used for higher-value anchoring/settlement events if needed. A permissioned chain is worth considering only if the compliance path requires transfer restrictions strict enough that a permissioned ledger meaningfully simplifies enforcement — otherwise it sacrifices liquidity/composability for little benefit.
- **Token standard**: this depends on the legal path (see legal doc). If treated as a security, an ERC-1400/ERC-3643-style permissioned security-token standard (whitelisted transfers, on-chain compliance hooks) is appropriate. If treated as a non-security digital collectible with attached revenue rights (Royal's path), a simpler ERC-1155 (semi-fungible, good for tiered ownership levels) or ERC-20-per-catalogue token can work, but this doc does not take a position on which legal path is right — that's a legal/business decision, not a technical one.
- **Distribution contract**: a claims-based payout contract (holders call `claim()` to pull their share of the latest distribution) is simpler and cheaper than push-based transfers, and is the pattern used by most token dividend/staking-reward systems today.

**2.5 Compliance layer**
KYC/AML on investor onboarding (via a provider like Sumsub or Persona), transfer restrictions enforced either on-chain (whitelist-gated transfers) or off-chain (a permissioned marketplace that only allows verified wallets to trade), and tax reporting (royalty income is taxable income for holders — the platform will likely need to issue equivalent-of-1099/CU-style statements depending on jurisdiction).

**2.6 Front end / marketplace**
A standard web app for browsing songs/catalogues, buying fractional tokens (fiat on-ramp needed — most retail users won't arrive with crypto already), viewing royalty statements, and claiming distributions. This is the least novel part of the stack and can be built with conventional tooling once the above is settled.

**2.6.1 Migrated to Angular (28 Aug 2026).** The frontend was rebuilt from the original single-file HTML/vanilla-JS prototype into a proper Angular application (`webapp/`), at full feature parity — marketplace, asset detail (all tabs, buy panel, yield-info popover), portfolio, redeem, KYC/MiFID appropriateness, artist onboarding (including the Luxembourg contract step), artist dashboard, theme toggle, and the 5-language i18n system. Key structural decisions: `@ngx-translate/core` (runtime language switching with no reload, matching the prior UX — Angular's built-in `@angular/localize` would have required separate builds per locale and a reload/redirect to switch, a regression), Angular Router with hash-based locations (`withHashLocation()`) to keep the existing `#/marketplace`-style URLs and sidestep GitHub Pages SPA-routing issues entirely, and the build's output path pointed directly at `docs/` so GitHub Pages' existing "deploy from `/docs`" setting didn't need to change. `server/` (the Node backend) and `contracts/` (the Hardhat project) are unrelated to this change and untouched — the Angular app talks to the same API endpoints via `HttpClient` that the old vanilla-JS app talked to via `fetch()`, with the same graceful-fallback-to-mock-data behavior when the backend is unreachable. i18n key parity was verified 294/294 across all 5 locale JSON files against the prior dictionary. Not yet verified: an actual in-browser click-through — no headless or real browser was available in the environment this was built in, so verification was build success + static/API smoke tests + code-level tracing, not live interaction. Worth a manual pass in a real browser before treating this as fully proven.

**2.7 Pre-production financing variant: milestone escrow + governance module**
If the platform supports funding *unfinished* tracks (see concept doc §8) rather than only tokenizing already-monetized catalogues, two additional components are needed, and they should be designed together since one bounds the other:

- **Milestone-gated escrow.** Funds raised for a track should sit in a smart contract, not a commingled operating account — this is a direct, checkable answer to the failure mode that sank PledgeMusic (a pre-blockchain platform that collapsed in 2019 after using new campaigns' money to cover old ones). A simple version: define 3–4 milestones per campaign (e.g., funding goal reached → studio/collaborators booked → mix/master delivered → release confirmed on DSPs), release a pre-agreed tranche of the escrowed funds at each on confirmation from the SPV manager or an appointed administrator, and build in an automatic refund path if the funding goal isn't met by a deadline or the artist doesn't deliver. **The refund is net of milestones already completed, not a full clawback**: `refundable = total_contributed − Σ(tranches already released for confirmed milestones)`. If, say, the studio-booking and mix-delivery milestones were met and paid out before the campaign stalled before "release confirmed," contributors are refunded pro-rata only the unreleased remainder — the artist keeps what was legitimately spent reaching the milestones actually delivered. This needs a clear dispute path too: what happens if a tranche was drawn but the milestone wasn't genuinely met is a job for the SPV manager/administrator (see legal doc) to adjudicate, not something the smart contract can determine on its own. This escrow design is a genuinely stronger guarantee than a traditional crowdfunding platform can offer, and worth stating plainly in investor-facing material as a differentiator.
- **Governance module — deferred to a later phase, and advisory-first when it ships.** Per the concept and legal docs, binding on-chain votes that directly execute business decisions (hiring a studio, choosing a performer) carry real, current legal exposure (the Lido DAO partnership-liability precedent) and a real product risk (design-by-committee outcomes). This is confirmed as a Phase 2+ feature (§3), not part of the pilot — Phase 0–1 ship with no token-holder voting at all, so the harder, more foundational problem (verified royalty data → escrow → distribution) gets proven first without also debugging a novel governance/liability structure. When it does ship, the lower-risk version: token holders vote on a *shortlist* the artist/platform has already vetted (so there's no single opaque gatekeeper, but also no unbounded crowd decision), the vote result is advisory, and the SPV manager formally executes and signs contracts — with the vote and its outcome recorded on-chain for transparency either way. A vote-weight cap per wallet (or quadratic weighting) is a hard requirement at launch, not a later hardening pass, so the mechanism doesn't just recreate concentrated, opaque control at the level of a few large token holders instead of solving it.
- **Production-milestone log doubles as the AI-disclosure record.** Each milestone confirmation above (studio booked, which musicians/vocalist performed, what mixing/mastering was applied, by whom, when) should be captured as structured, timestamped data, not just a payment trigger. That same record is exactly what's needed to (a) auto-generate accurate DDEX AI-disclosure metadata for DSP submission instead of relying on the artist to self-report it correctly, and (b) evidence how much human creative/technical contribution a track received if a DSP's classification is ever disputed — directly supporting the risk-transformation argument in concept doc §7.1 (documented human production reduces, but per current DSP policy does not guarantee against, AI-demonetization exposure). Building one system that serves the escrow, the disclosure metadata, and the provenance record is simpler and more defensible than three separate ones.

**2.8 Authorship/anti-plagiarism registry (proposed, 28 Aug 2026) — an extension of §2.7's log, not a separate product.** Idea raised: build a public registry of uploaded tracks usable as evidence in artist-vs-artist plagiarism disputes. Two things worth being precise about before building it:
- **It does not touch the SIAE/CMO friction described in the legal doc (§4, §7.4).** Royalty collection (a regulated collective-rights-management activity) and proof-of-authorship timestamping (an unregulated evidentiary service — comparable to SIAE's own "deposito opere inedite" or the existing Italian service Patamu) are legally distinct functions. This registry doesn't reduce the work needed to resolve royalty-assignment terms with whichever society administers a given catalogue.
- **Evidentiary strength depends on the timestamp mechanism, not just having a database.** Italian copyright arises automatically at creation (no registration needed); what a plagiarism dispute actually turns on is *proving priority date*. A plain internal DB row with a timestamp is weak evidence (self-certified, contestable). An eIDAS-qualified electronic timestamp (Regulation (EU) 910/2014), issued by a Qualified Trust Service Provider, carries an EU-wide legal presumption of accuracy for date and data integrity — materially stronger than a bare blockchain anchor alone, though anchoring on top is a reasonable tamper-evidence complement. Neither a timestamp nor a blockchain anchor *proves* original authorship on its own (someone could timestamp a plagiarized work too) — it's supporting evidence a court weighs, not a verdict.
- **Build it as an extension of the existing per-track record**, not a new system: at upload/onboarding, hash the submitted audio/lyrics/stems and timestamp that hash using the same structured log described in §2.7 (ideally via an eIDAS-qualified timestamp service), rather than standing up an independent registry product. This keeps one provenance record serving escrow milestones, AI-disclosure metadata, and authorship evidence, consistent with §2.7's "one system, not three" principle.

**2.9 Projected yield — now computed, not hardcoded (28 Aug 2026).** The "X% proj. yield" figure shown on catalogue listings previously lived as a static number in the mock data, disconnected from the royalty history chart shown right next to it. It's now derived: `yield% = (royalty paid out over the trailing 12 reported months ÷ (tokenPrice × tokensTotal)) × 100` — implemented as `computeYieldBreakdown()`/`computeProjectedYield()` in `docs/index.html`, with a matching info popover on the asset detail page showing the formula, the actual numbers behind the current figure, and an explicit disclaimer that this is a trailing/historical figure, not a promised or guaranteed return. Fixing this exposed that the mock royalty-history generator's base amounts had never been calibrated against `tokenPrice × tokensTotal` — computed yields came out at 37–71% before the seed data's `buildRoyaltyHistory()` base parameters were rescaled to reproduce plausible single-digit yields. A real implementation would compute this the same way from real royalty-administrator statements, not change the underlying principle.

**2.10 First real on-chain code — testnet demo token contract (28 Aug 2026).** `contracts/` is a Hardhat project with `HumfiverseCatalogueToken.sol`, an ERC-1155 contract (OpenZeppelin v5) with one token id per catalogue-kind demo track (`scripts/catalogues.js` — midnight-static, ember-choir, paper-cranes, copper-radio; the two preproduction/milestone-escrow assets are intentionally excluded, since those need the separate escrow mechanism in §2.7, not simple issuance). `mintCatalogue(tokenId, slug, supply)` mints the catalogue's full `tokensTotal` into the contract's own balance — the "platform pool" — using OpenZeppelin's `ERC1155Holder` so the contract can safely hold its own tokens (plain `_mint` to `address(this)` reverts without it — a real bug hit and fixed while building this, not a hypothetical). `releaseFromPool(to, tokenId, amount)` is the only way tokens leave the pool, owner-gated, tracking `releasedOf` against `totalSupplyOf` so it can never over-release. No payment/purchase logic, no KYC/transfer-restriction enforcement — this is purely the issuance-and-pool mechanism, not a compliant offering (§2.4's ERC-3643-style permissioned standard is still what a real security-path launch would need). 8 Hardhat tests cover minting, double-mint protection, owner-gating, pool release/over-release, and multi-catalogue independence — all passing, plus a full deploy→mint dry run against a local Hardhat network before any testnet involvement. Configured for Base Sepolia (consistent with the "low-fee L2" pick in §2.4).

**Deployed to Base Sepolia (28 Aug 2026).** Contract live at [`0xC1aFD3D24de2C344053bBe83aB412140C452146b`](https://sepolia.basescan.org/address/0xC1aFD3D24de2C344053bBe83aB412140C452146b), deployed from a user-supplied, testnet-only throwaway wallet. All four catalogues minted into the pool in the same session: midnight-static (4000), ember-choir (2500), paper-cranes (5000), copper-radio (3200). Total gas cost for deploy + 4 mints: ~0.000012 ETH (Base Sepolia gas was ~0.006–0.011 gwei at the time) — confirms the "low-fee L2" pick in §2.4 in practice, not just in theory. `releaseFromPool` has not been called — every catalogue's full supply still sits in the pool untouched; no addresses have received tokens yet.

**On-chain data wired into the interface, and new-track minting automated (28 Aug 2026).** `server/chain.js` is the one real (non-mock) dependency this backend has — `ethers`, needed to actually read from and sign transactions against the deployed contract. Two new endpoints: `GET /api/onchain/:assetId` (public read — pool balance, total supply, released amount, live from the chain, no key needed) and `POST /api/onchain/mint` (owner-gated write — needs `CHAIN_OPERATOR_PRIVATE_KEY` set to the contract-owner wallet's key; without it, minting is disabled but reads and everything else still work, same graceful-degradation pattern as the rest of the backend). A new `onchain_tokens` table maps `assetId → tokenId`, seeded at first boot with the four catalogues already minted via `contracts/scripts/mintCatalogues.js` (including their real historical tx hashes) so the API reflects the true on-chain state from day one, not just what this server itself has minted.

The Angular artist-onboarding wizard now calls the mint endpoint automatically right after creating a new catalogue-kind campaign (`onboarding.component.ts`'s `submit()`), using the exact `tokensTotal` set by the campaign parameters in the UI as the on-chain supply — preproduction campaigns are excluded, matching the contract's scope. The asset-detail page shows a live "on-chain token" panel (token id, pool/total-supply, a link to the block explorer) whenever an asset has one, fetched fresh from the chain on each page load rather than cached.

**A real bug found and fixed while wiring this up**: the token-id counter (`MAX(token_id)+1` in the local SQLite table) is only a hint, not a source of truth — a local DB reset (which happened during testing) desynced it from what was actually already minted on-chain, and the next mint attempt reverted with "already minted." Fixed by having the mint path verify on-chain (`totalSupplyOf(tokenId) == 0`) before committing to a candidate id, advancing past any occupied one — the chain is authoritative, the local table is just bookkeeping for fast reads.

**Not yet done**: the production backend on Render does not have `CHAIN_OPERATOR_PRIVATE_KEY` (or the other `CHAIN_*` vars) configured yet — minting is currently only verified locally. Setting that up on Render puts a real (if low-value, testnet-only) private key into a third-party host's environment variables; this needs the user's explicit go-ahead, not something to do unilaterally.

**2.11 Secondary-market fee infrastructure — HumfiverseMarketplace.sol (29 Aug 2026).** Business rule requested: every platform-mediated token sale *other than* a first purchase retains a 1% platform fee, taken in tokens rather than sale proceeds. Rather than adding an "is this a first purchase?" check somewhere, the fee falls out of keeping two contracts strictly separate:
- `HumfiverseCatalogueToken.releaseFromPool` (§2.10) remains the *only* path for a first purchase — platform pool → buyer, owner-gated, no fee, unchanged.
- `HumfiverseMarketplace.sol` (new) is a non-custodial peer-to-peer listing contract: a holder who already owns tokens (i.e., by construction, someone past their first purchase) calls `list()` after `setApprovalForAll`-ing the marketplace on the token contract; a buyer calls `buyListing()` and pays native testnet ETH. `PLATFORM_FEE_BPS = 100` (1.00%) of the *token amount* — not the payment — is diverted to `feeRecipient` on every purchase; the seller is still paid in full for everything sold, so the fee comes out of what the buyer receives, not the seller's proceeds. Fee math is `floor(amount × 100 / 10000)`, so trades under 100 tokens round the fee down to 0 — an accepted consequence of an integer token-count fee, documented in the contract rather than worked around. Listings are non-custodial (tokens stay in the seller's wallet until an atomic buy transaction moves them), and `buyListing` follows checks-effects-interactions plus OpenZeppelin's `ReentrancyGuard`. 22 Hardhat tests cover listing/approval/balance checks, the fee split and rounding, full-payment enforcement, listing cancellation, and fee-recipient management — all passing. **Not yet deployed** to Base Sepolia — this is source + tests only, matching how the token contract itself started (§2.10) before a deploy was explicitly requested.
- **Frontend**: Portfolio gained a "Sell" action per holding (only shown when `tokens > 0`, so it's structurally impossible to sell before a first purchase) that mirrors the contract's exact fee formula client-side against the app's existing mocked-portfolio state (this repo's purchase flow has never done real on-chain settlement per-trade — see `asset-detail.component.ts`'s `buy()` — so the sale flow matches that same simulated fidelity rather than introducing real settlement for only one side of the trade). The confirmation modal shows the token quantity, proceeds, and the platform's token fee before the seller confirms, and the i18n copy explaining the rule ships in all 9 supported locales.

**2.12 "Verifiable on-chain" + a real market price — resolved as listing-based resale, not dynamic/algorithmic pricing (29 Aug 2026).** Follow-up request: make the purchase flow "verifiable on-chain" and show a token's price rising as it's bought. Two things got untangled here, one technical and one already-decided-in-this-repo:
- **On-chain-verifiable purchase (technically fine, not yet built)**: `WalletService` today deliberately never requests a signature or transaction (see its own doc comment) — every "buy" in this app, including §2.11's `HumfiverseMarketplace`, is UI-state-only. Making the *primary* purchase a real signed transaction against `HumfiverseCatalogueToken` would be a clean, low-risk extension (a public, priced `buy()` alongside the existing owner-gated `releaseFromPool`) — not done in this pass, flagged for later if wanted.
- **Automatically-rising/algorithmic price (deliberately not built — already decided against in legal-regulatory-notes.md §7.8)**: that note is explicit that a fluctuating price formed by an automated matching mechanism (bonding curve, continuous order book) crosses from "issuer/placement agent" into **MTF/OTF authorization under MiFID II Title II** — a heavier, separate license — which is exactly why §2.3's distribution-vault design and this repo's whole "simulate secondary-market price movement" stance exist. Proposing to apply for MTF authorization directly is a real, multi-month/multi-year regulated-infrastructure undertaking for the business/legal side to pursue with counsel — not something achieved by writing contract code, and orthogonal to this prototype.
- **What shipped instead**: the resale mechanism from §2.11 (`HumfiverseMarketplace`-style listings, mirrored client-side) already lets holders resell — the earlier "tokens aren't resellable" read on the product was inaccurate, just not yet visible in the UI. Added a `SecondaryListing` model + `StoreService.secondaryListings`/`activeListingsFor()`/`lowestAsk()`, seeded with a few mock listings (including one on the already-sold-out `ember-choir` catalogue, at $46 against its $40 primary price — a concrete, honest demonstration of resale price appreciation without any automated pricing mechanism). Portfolio's "Sell" now creates a real listing (seller sets their own price, exactly like `HumfiverseMarketplace.list()`) instead of an instant simulated sale, with a "Your listings" section to cancel one; Asset Detail gained a "Resale marketplace" panel showing active listings, a "Market price" stat tile (the cheapest active listing, i.e. **the lowest active listing method the user confirmed**, not an aggregate/weighted price), and a "Buy" action per listing that applies the same 1% token fee from §2.11. The fee constant was pulled into a shared `core/marketplace-fee.util.ts` so Portfolio and Asset Detail can't drift apart on the formula. i18n shipped in all 9 locales.

**2.13 Real on-chain purchases — fixed-price `buy()`, deliberately not dynamic pricing (29 Aug 2026).** Follow-up to §2.12: the user confirmed wanting real, wallet-signed on-chain purchases (not just verifiable state). Implemented as a genuine primary-sale path, kept strictly apart from the pricing question already resolved in §2.12/legal-regulatory-notes.md §7.8:
- **`HumfiverseCatalogueToken.sol`**: added `pricePerToken` (wei, per token id, 0 = not for public sale), a public `payable buy(tokenId, amount)` that charges exactly `amount × price`, releases from the same pool/`releasedOf` accounting as `releaseFromPool`, and forwards proceeds to a configurable `payoutRecipient` (owner-settable, defaults to deployer) — plus owner-only `setPrice`/`setPayoutRecipient` and `ReentrancyGuard` on the paid path. `mintCatalogue` now takes a price argument. This is a breaking signature change to an *unaudited testnet contract that isn't load-bearing yet*, not a production upgrade — 17 new/updated Hardhat tests (31 total across both contracts, all passing) cover the buy path, pool-accounting parity with `releaseFromPool`, and the admin functions. Verified end-to-end against a local Hardhat node (not just unit tests): deployed, minted, and executed a real `buy()` transaction with the exact ethers.js call shape `WalletService.buyOnchain` uses — correct token delivery, exact ETH forwarded to the payout recipient, pool decremented correctly.
- **Redeployed to Base Sepolia (29 Aug 2026, on the user's explicit go-ahead — the classifier that gates broadcasting transactions blocked the first unprompted attempt, correctly, and only proceeded once the user directly asked for it)**: new contract at [`0xFa9CCBCAAbd08f8f189249A82B0808dcb05e99c2`](https://sepolia.basescan.org/address/0xFa9CCBCAAbd08f8f189249A82B0808dcb05e99c2), replacing the old `0xC1aFD3D24de2C344053bBe83aB412140C452146b` (§2.10 — now historical/unused, nothing of value was on it). All four catalogues re-minted with real prices: midnight-static (4000 @ 0.0025 ETH), ember-choir (2500 @ 0.004 ETH), paper-cranes (5000 @ 0.0015 ETH), copper-radio (3200 @ 0.003 ETH) — verified directly against chain state (`totalSupplyOf`/`poolBalance`/`pricePerToken` read back correctly for all four; the mint script's own end-of-run balance printout showed a stale `0` for the last one, a public-RPC read lag right after its tx, not a real gap — re-querying moments later confirmed the correct pool balance). `server/chain.js`'s default `CONTRACT_ADDRESS` and `server/.env.example` updated to the new address so a fresh environment without `CHAIN_CONTRACT_ADDRESS` set still points at the right contract.
- **Still not configured on Render**: `CHAIN_OPERATOR_PRIVATE_KEY` (same value as `contracts/.env`'s `DEPLOYER_PRIVATE_KEY` — it must be the contract owner's key, and this testnet demo uses one wallet for both roles) and `CHAIN_CONTRACT_ADDRESS` (the new address above) still need to be set in Render's dashboard by the user — this repo has no Render API access, so that step can't be done from here.
- **`server/chain.js` / `server/server.js`**: ABI updated for the new `mintCatalogue` signature and a `pricePerToken` read; `GET /api/onchain/:assetId` now returns `priceWei`; `POST /api/onchain/mint` accepts an optional `priceWei`. Not yet configured on the live Render deployment (confirmed by hand: hitting the production API's `/api/onchain/:assetId` returns a bare 404, meaning the deployed backend predates even the §2.10 on-chain-read feature — Render hasn't picked up recent pushes, and `CHAIN_OPERATOR_PRIVATE_KEY` was never set there either, both still open items).
- **Frontend**: `webapp` gained `ethers@6` as a real dependency (only place in the Angular app that needs one). `WalletService` — whose own doc comment previously said "no signature or transaction is ever requested" — now has `ensureBaseSepolia()` (switches/adds the network via wallet RPC calls) and `buyOnchain()` (opens a `BrowserProvider`, requests a signature for `buy(tokenId, amount)`, waits for the receipt). This is the *only* path in the app that does this — redeem, resale listings, and preproduction contributions all stay simulated. `asset-detail.component.ts`'s `buy()` now branches: if the asset has a live on-chain price *and* a connected wallet, it submits the real transaction (updating the local mocked portfolio afterward too, matching this repo's established pattern of layering real on-chain state on top of, not replacing, the mock bookkeeping the rest of the app reads — see §2.10's mint endpoint for precedent); otherwise it falls back to the existing simulated purchase, so the app keeps working with no wallet connected. The buy panel shows a live "real purchase" indicator or a connect prompt depending on state, and the success modal shows the real tx hash with an explorer link when applicable. i18n shipped in all 9 locales. `Asset.tokenPrice` (USD, mock display) and the contract's `pricePerToken` (wei, real) are intentionally two different numbers connected only by an illustrative 0.0001 ETH-per-$1 mapping applied at mint time (`onboarding.component.ts`, mirroring `contracts/scripts/catalogues.js`) — there is no real USD/ETH peg here.

**2.14 Marketplace listings are now chain-native, and preproduction campaigns get a token too (29 Aug 2026).** Two coupled decisions from the same conversation:
- **Unified token issuance.** Previously only catalogue-kind campaigns got an on-chain token at creation — preproduction (milestone-escrow) ones were excluded because §2.7's escrow contract, which was supposed to control fund release for that model, was never built (see §2.13's note on this). The user made the call to unify: every campaign, catalogue or preproduction, now gets a `mintCatalogue`/`buy()`-eligible token the moment it's created (`onboarding.component.ts`'s `submit()` no longer gates the mint call on `!isPre`). **This is an explicit simplification, not a resolved regulatory question**: for preproduction specifically, §4 of legal-regulatory-notes.md flags real, unresolved territory (ECSPR vs. full MiFID II, possible AIFMD collective-investment-scheme classification) that a real launch would need counsel on, and the token alone doesn't solve the PledgeMusic-style pooled-funds-custody problem §4 also raises — that still needs the milestone escrow controlling *fund* release, not just a token controlling *claim* issuance. Fine for a testnet demo whose whole premise is "not for real funds, not audited" — flagged here so it isn't mistaken for a resolved design later.
- **Chain-native marketplace listings.** Rather than trusting `assets.json` (or the backend's mock/DB copy of it) to decide what's for sale, the Marketplace now shows only assets with a real, verified on-chain token — the intent being that this reduces how much "is this real" logic lives in the frontend/mock layer versus the chain itself, a step toward the eventual migration path.
  - `server/chain.js`'s `listMintedSlugsFromChain()` reads every `CatalogueMinted` event directly off the contract (paginated in ~9000-block windows from the deploy block, since public RPCs cap `eth_getLogs` at 10,000 blocks) — the local `onchain_tokens` table is a convenience index, not the source of truth here. `GET /api/onchain/list` serves this, falling back to the local table only if the chain read itself fails.
  - `StoreService.onchainAssetIds` loads this once at boot (`null` while loading/unreachable); `MarketplaceComponent.chainVerifiedAssets` filters `store.assets()` down to it, degrading gracefully to the full mock catalogue if the check never resolves — same fallback philosophy as the rest of this backend. `onboarding.component.ts` adds a freshly minted campaign's id to that set immediately after a successful mint, so a brand-new listing doesn't vanish from its own marketplace while waiting for the next full reload.
  - **Backfilled** the two existing preproduction demo assets (`glass-horizon`, `low-tide-orchestra`) with real mints against the redeployed contract, so they didn't just disappear once the filter shipped — verified live: `GET /api/onchain/list` on the production API now returns all six seed assets (plus one `test-mint-claude` throwaway used to verify the mint path end-to-end, harmless — it has no `assets.json` entry so it never renders a card).

**2.15 Milestone escrow, built for real — HumfiverseMilestoneEscrow.sol (30 Aug 2026).** §2.7 described this mechanism in prose since the planning phase; it's now real code, not just a design note. The user's specific framing: the escrow should make the artist's commitment to a pre-agreed studio *concrete*, with Humfiverse as guarantor — not just a promise, and Humfiverse holds the final call on whether a milestone genuinely happened (confirmed directly by the user, closing the one open design question).
- **Contributions never reach the artist directly.** `contribute(campaignId)` holds ETH in the contract itself — a direct, checkable answer to the PledgeMusic commingled-funds failure mode cited in legal-regulatory-notes.md §4.
- **The studio commitment is enforced by fund routing, not by trust.** A small platform-curated studio registry (`registerStudio`/`setStudioActive`, owner-gated) holds each studio's wallet address. A campaign's "studio booked" milestone is tagged `Payee.STUDIO` at creation — when Humfiverse confirms that milestone, the tranche is paid straight to the studio's registered wallet. The artist's own wallet is never in that payment's path. Every other milestone (funding goal reached, mix & master, release confirmed) pays the artist as general production costs.
- **Humfiverse confirms milestones, full stop.** `confirmMilestone` is `onlyOwner` — same operator key already used for `HumfiverseCatalogueToken`/`HumfiverseMarketplace`. This matches the "attested, not trustless" posture stated throughout this doc (§2.3, §2.13) rather than inventing a new trust model for this one contract.
- **Refunds are pro-rata on the unreleased remainder, not a full clawback**, implementing §2.7's formula literally: if a campaign is cancelled, each contributor can `refund()` their contribution scaled by the fraction of the funding goal that was never released to a confirmed milestone — money already paid out for milestones genuinely delivered stays paid.
- 20 new Hardhat tests (52 total across all three contracts, all passing) — including a test that specifically proves the studio-commitment milestone pays the studio's balance and leaves the artist's untouched, the mechanic the whole feature was requested for.
- **Not yet deployed, not yet wired to the backend/frontend.** Mirrors how `HumfiverseMarketplace.sol` shipped contract-first in §2.11 before its UI followed in §2.12. Open next steps, not yet decided: (a) deploy to Base Sepolia; (b) does the onboarding wizard need a real "pick a studio" step, and who's allowed to register a studio into the registry (platform-only for now, per the contract); (c) does contributing to a preproduction campaign become a real signed transaction (mirroring §2.13's `buy()` wiring) or stay simulated like the rest of the mocked purchase flow; (d) a milestone-confirmation surface doesn't exist anywhere in this app yet — today nothing plays the "Humfiverse confirms" role except direct contract calls.

**2.16 Milestone escrow wired end-to-end: real contributions, deployed, and an admin confirm panel (30 Aug 2026).** Follow-up to §2.15: the user asked for contributions to also be real on-chain transactions, and for the "Humfiverse confirms milestones" role to have an actual interface.
- **Deployed** `HumfiverseMilestoneEscrow` to Base Sepolia at [`0x51090e5767F67aFc52725B2267f409811d59d03d`](https://sepolia.basescan.org/address/0x51090e5767F67aFc52725B2267f409811d59d03d) (`contracts/scripts/deployEscrow.js`). Verified against the live deployment (not just Hardhat's local network): registered a studio, created a campaign, contributed real testnet ETH, and confirmed the `confirmMilestone` guard correctly reverts with `"not enough raised for this tranche"` when it should — the same check the 52 Hardhat tests already covered, now proven against the actual deployed bytecode too.
- **`server/chainEscrow.js`** (new, separate from `chain.js` since this is a second, independent contract): `registerStudioOnchain`, `createCampaignOnchain`, `confirmMilestoneOnchain`, `getCampaignInfo`. Two new tables (`escrow_campaigns`, `escrow_studios`) index campaigns and dedupe studios by wallet so the same studio isn't re-registered across campaigns. New endpoints: `POST /api/escrow/campaign` (creates a campaign — registers the studio first if new), `GET /api/escrow/campaign/:assetId`, `GET /api/escrow/campaigns` (admin listing), `POST /api/escrow/confirm`.
- **Onboarding wizard**: preproduction campaigns now collect a studio name + wallet address (validated as a well-formed address) on the budget step — this is what the escrow's studio-payee milestone routes its tranche to. The review step shows whether the connected wallet will make this a real escrow (matching the pattern already established for catalogue purchases in §2.13) or prompts to connect one; without a wallet, the campaign is still created (token included) but the escrow is skipped with a toast explaining why, rather than blocking creation outright.
- **Real contributions**: `WalletService.contributeOnchain()` mirrors `buyOnchain()` — same network-check/signature-request pattern, calling `HumfiverseMilestoneEscrow.contribute()` instead of `HumfiverseCatalogueToken.buy()`. `asset-detail.component.ts`'s `buy()` now branches three ways: real escrow contribution (preproduction, active campaign, wallet connected) → real catalogue purchase (§2.13's existing path) → simulated fallback, in that order.
- **Admin panel** at `/admin/escrow` (`AdminEscrowComponent`) — deliberately unlinked from the main nav and not translated into the 9 public locales, since it's an internal operator tool, not part of the artist/investor surface. Lists every campaign with its milestones, funding progress, and a "Confirm" button per unreleased milestone that's disabled until enough has actually been raised to cover it (mirroring the contract's own guard, so the UI doesn't invite a doomed transaction).
- Drive-by fix: found `&amp;` HTML-entity artifacts leaking into plain (non-`innerHTML`) English UI text (`forArtists.kicker`, `wizModel.preprodBody`, `wizSource.mixLabel`, `wizReview.title`) while screenshot-testing the wizard — fixed since it was a one-line, zero-risk correction spotted in-session, not a new investigation.
- **Known gap**: full click-through of the wizard's contract-acceptance step wasn't verified in headless Playwright — the custom-styled checkboxes didn't register `.check()` reliably in that automation, which reads as a test-tooling limitation (the app correctly blocked submission and showed the right validation toast) rather than a confirmed app bug, but it's not proven clean either. Worth a manual pass.

**2.17 A fixed, per-asset pointer from the site to the governing contract on the block explorer (30 Aug 2026).** The user's ask: every campaign's token — and its on-chain governance/escrow, for preproduction — should have a visible, permanent link from the site to the explorer, so anyone can independently verify the contract that actually controls it.
- Asset Detail already had this for the token contract (§2.10); it was missing for the milestone escrow specifically. Added an "Milestone escrow contract" panel (preproduction assets only, when a campaign exists): campaign ID, raised/goal, the assigned studio, and two links — **View on explorer** (the deployed contract address on Basescan) and **View source code** (the actual `.sol` file on GitHub).
- Also added the source-code link next to the existing token-contract explorer link, for the same reason.
- **Attempted contract source verification on Basescan via Sourcify** (free, no API key, unlike Etherscan-style verification) so the explorer itself would render readable Solidity instead of just bytecode. Hit a real wall: this repo's `hardhat-verify` (2.1.3, pinned to Hardhat 2.x) calls Sourcify's `check-all-by-addresses` endpoint, which now 404s — Sourcify's public API has moved on since that plugin version was written, confirmed by `curl` (their `/chains` endpoint still works, `/check-all-by-addresses` doesn't). The fix (`hardhat-verify@3.x`) requires Hardhat 3, a major-version upgrade of the whole toolchain — not attempted, since it's real migration risk (config format, ESM) for a verification nicety, not requested on its own. `sourcify: { enabled: true }` is left in `hardhat.config.js` for whenever that upgrade happens; in the meantime, the GitHub source links are the honest substitute — the actual governance logic is one click away either way, just not rendered inside Basescan's UI yet.
- Backfilled a real escrow campaign for `glass-horizon` (one of the pre-existing preproduction demo assets) so the new panel had real on-chain data to show immediately, same pattern as earlier backfills (§2.14).

**2.18 Discovered: Render's free-tier disk is ephemeral — the backend's local DB doesn't survive a redeploy (30 Aug 2026).** While checking the new panel above in production, `GET /api/onchain/glass-horizon` came back `{"onchain": false}` — despite having backfilled that exact record earlier the same session. Root cause: `render.yaml` declares no persistent `disk:` resource, so every push-triggered redeploy gets a fresh container filesystem; `server.js`'s `seedIfEmpty()` only re-inserts the four hardcoded catalogue `onchain_tokens` rows on an empty table, so anything written later at runtime — backfilled preproduction tokens, every `escrow_campaigns`/`escrow_studios` row, KYC records, contract acceptances — is only as durable as "until the next deploy." The chain itself is unaffected (it's the whole point of §2.14's design), but the backend's local mirror of it isn't.
- **Fixed the part that can be fixed without touching Render's plan**: `GET /api/onchain/:assetId` now falls back to `chain.listMintedSlugsFromChain()` (the same event-log read `GET /api/onchain/list` already used) when the local table misses, and opportunistically re-inserts what it finds — self-healing the cache from the real source of truth. Verified locally against a wiped DB: correctly recovered `glass-horizon`'s real token id and mint tx hash from chain events alone.
- **Not fixable the same way**: escrow campaigns. `HumfiverseMilestoneEscrow`'s `CampaignCreated` event has no `assetId`/slug field — the asset↔campaignId link only ever existed in `escrow_campaigns`, so a wiped table means that link is genuinely gone until re-created, not just uncached. Two real options, not decided here: (a) attach a persistent disk to the Render service (likely a paid-tier change — a billing decision for the user, not made unilaterally), or (b) extend the escrow contract to accept and store an asset identifier on-chain the same way the token contract does, which is a contract redesign + redeployment, same category of change as §2.13/§2.15/§2.16.
- Practical consequence right now: the `glass-horizon` escrow campaign and backfilled preproduction token demonstrated in §2.17 may already be gone by the time this is read, if any further deploy happened since. Re-running the same backfill calls restores them; nothing on-chain was lost.

**2.19 The asset<->campaign link now lives on-chain — option (b) from §2.18, not the paid-disk route (30 Aug 2026).** The user picked the contract-side fix over paying for a persistent Render disk (confirmed: Render disks are $0.25/GB/month but require a paid instance plan — Free-tier services can't attach one at all, so the realistic minimum is the $7/mo Starter plan plus disk, not just the $0.25).
- `HumfiverseMilestoneEscrow.Campaign` gained an `assetId` field, `createCampaign` now takes and stores it (reverting with `"asset already has a campaign"` on a duplicate), and a new public `campaignIdByAssetId(string) → uint256` mapping gives an **O(1) on-chain lookup with no event scanning needed** — a stronger fix than the token side got in §2.18, which still has to scan `CatalogueMinted` logs because that event has no asset identifier. `CampaignCreated` now also carries `assetId`. 3 new tests (55 total across all three contracts): empty-assetId rejected, duplicate-assetId rejected, on-chain lookup round-trips correctly.
- Redeployed to `0xbc220eB1234749a2127aad3deB69c689DC74C1a1` (superseding §2.16's `0x51090e...9d03d` — not upgradeable in place, same as every prior contract change in this doc).
- `server/chainEscrow.js`: `getCampaignInfoByAssetId()` calls `campaignIdByAssetId` directly — `GET /api/escrow/campaign/:assetId` no longer touches the local table at all. `listCampaignAssetIdsFromChain()` (event-scan, mirroring the token side's `listMintedSlugsFromChain`) backs `GET /api/escrow/campaigns` too, so the admin panel's listing also survives a wiped local DB. The local `escrow_campaigns`/`escrow_studios` tables are now purely a cache/optimization, not a dependency — verified locally against a freshly wiped DB: both the single-asset lookup and the admin listing recovered full, correct data from the chain alone.
- Studio de-duplication (`escrow_studios` keyed by wallet) is still a local-table-only optimization — a wiped DB means a studio's wallet could get re-registered under a new `studioId` on a later campaign. Left as-is: harmless (each registration is still valid, distinct, and correctly routes its payout), just not maximally tidy on-chain. Not fixed here.

**2.20 A bigger gap than the disk: user-created campaigns were never persisted server-side at all (30 Aug 2026).** While answering the user's question about whether a paid Render disk is needed for the site↔blockchain link (answer: no — §2.18/§2.19 already made that link disk-independent), a separate and more basic problem surfaced: the onboarding wizard's `submit()` only ever wrote a newly-created asset/campaign into `StoreService`'s in-memory signals. Nothing sent it to the backend, so a campaign a user just created existed only in that one browser tab — a refresh, a different device, or another visitor's browser would never see it, with or without a persistent disk. The user's ask was explicit: campaigns users create must be permanent and visible to everyone.
- **`POST /api/assets`** (new, `server/server.js`): takes `{asset, campaign?}`, requires `asset.id`/`title`/`kind`, rejects a duplicate id with 409, inserts into the existing `assets`/`campaigns` tables (same tables `seedIfEmpty()` already used for the four hardcoded demo catalogues — user-created campaigns now live alongside them, not in a separate path). `getAssetById()` added as a small helper for the duplicate check.
- **`ApiService.createAsset()`** (new) posts to it; `onboarding.component.ts`'s `submit()` calls it right after the existing local `store.assets.update()`/`store.campaigns.update()` writes, gated on `store.backendAvailable()` and best-effort (`.catch()` shows a `toast.assetSaveFailed` translated in all 9 locales rather than blocking the wizard — the on-chain mint and local UI state are the parts that must not fail; the server copy is important but the flow shouldn't hard-block on it, consistent with how §2.16 treats the escrow step as best-effort when no wallet is connected).
- **Verified two ways**: direct `curl POST` (asset appeared in a fresh `GET /api/data`, simulating a second visitor; duplicate id correctly 409'd) and a full Playwright run through the actual wizard UI (title/artist/model/checkboxes/launch), confirming the real network call fires (`200 POST /api/assets`) and the created asset is readable via an independent `curl` call after the browser tab that created it is gone — the same "does a second, unrelated visitor see it" test the user's requirement implies.
- **Scope note**: this makes newly-created campaigns durable in the same local-table sense §2.19 already accepted as sufficient for escrow/studio data — not on-chain. That's consistent with the rest of the app (`Asset`/`Campaign` records were never meant to be on-chain; only the token and, for preproduction, the escrow campaign are). Render's ephemeral disk (§2.18) still applies here exactly as it does to everything else in these tables — a wiped DB loses user-created campaigns the same way it can lose a backfilled token cache, with no on-chain fallback for asset metadata the way §2.18/§2.19 built for tokens/escrow. Not addressed in this pass; flagged for later if it matters (either a paid disk, or moving core asset metadata on-chain too).

**2.21 The one genuinely admin-only endpoint now requires a key (30 Aug 2026).** Prompted by a smoke-test row left in production after §2.20: the user asked whether a delete endpoint would fix a real problem before building one. It wouldn't — an unauthenticated delete would be a new hole, not a fix, since nothing on this backend had any auth at all. Auditing every write endpoint for who's actually meant to call it surfaced the real gap and the right scope for a fix:
- `POST /api/onchain/mint` and `POST /api/escrow/campaign` are triggered by the onboarding wizard for *every* normal user creating a campaign (§2.14, §2.16) — not admin actions, so gating them behind a server-held secret would either break normal campaign creation or require baking the secret into the public bundle, which isn't protection at all. Left open, same as `POST /api/assets` from §2.20 — the honest fix for spam/abuse on these is rate-limiting, a different and larger piece of work, not attempted here.
- `POST /api/escrow/confirm` is the one endpoint only Humfiverse should ever call — it releases real escrowed funds, and its only caller anywhere in this app is the internal `/admin/escrow` panel (§2.16). It had zero auth: anyone who found the URL could confirm any campaign's milestone and force a payout. Fixed with a shared-secret gate — `ADMIN_API_KEY` env var on the server, checked against a required `X-Admin-Key` header (`isAdminAuthorized()` in `server.js`); unset key means the endpoint fails closed (401/503-style, not silently open) rather than defaulting to no-auth. `AdminEscrowComponent` gained a password-style input that holds the key in that tab's `sessionStorage` only and sends it solely on this one call (`ApiService.confirmEscrowMilestone` now takes it as a third arg); confirm buttons disable until a key is entered. Verified locally: no header → 401, wrong header → 401, correct header → passes auth and reaches the real contract call. Not a full auth system (no rotation, no per-admin identity, key travels in a plain header) — proportionate to this being the one operator-only action in an otherwise-unauthenticated testnet prototype, not a production access-control design.

**2.22 Diagnosed: new campaigns silently missing from the marketplace, root-caused to the free public RPC rate-limiting mint calls (30 Aug 2026).** The user reported two symptoms after §2.20/§2.21 shipped: newly created tracks didn't show up in the marketplace, and campaign creation returned an error saying the token couldn't be created. Reproduced directly against production: `POST /api/onchain/mint` failed with `-32016 over rate limit` on `eth_getTransactionCount` — Base Sepolia's free public RPC (`https://sepolia.base.org`, the default `CHAIN_RPC_URL`) throttles under even a modest burst of requests, and this session's own testing (Playwright runs, curl smoke tests, the admin-panel checks) was enough to trigger it. Retrying the identical call seconds later succeeded, confirming it's transient, not a config or contract problem. The two symptoms are the same bug: `onboarding.component.ts`'s `submit()` already treats minting as best-effort (§2.14) so the campaign itself gets created and persisted (§2.20) even when the mint call fails — but the marketplace deliberately only lists chain-verified assets (§2.14's explicit design), so a campaign whose mint failed is invisible there even though it exists in the backend. The onboarding error toast ("couldn't create a token") was accurate, just not actionable — nothing retried a plainly transient failure.
- **Fix: `server/chainRetry.js`** (new) — a small `withRetry()` wrapper (3 attempts, exponential backoff starting at 1.5s) that only retries errors shaped like a transient RPC problem (rate limits, timeouts, network errors — checked by ethers error `code` and message content), and lets a genuine on-chain rejection (bad input, a real revert reason) fail immediately as before. Wired into every operator write call that showed up as a failure point: `chain.js`'s `mintCatalogueOnchain`, `chainEscrow.js`'s `registerStudioOnchain`/`createCampaignOnchain`/`confirmMilestoneOnchain`, and both modules' `queryFilter` event-scan loops (`listMintedSlugsFromChain`, `listCampaignAssetIdsFromChain`) since a rate-limited read there would make the marketplace or admin panel look emptier than it really is, same underlying cause.
- **Not fixed here, flagged as the more durable option**: switching `CHAIN_RPC_URL` from the free public endpoint to a dedicated provider (Alchemy/Infura both have a free Base Sepolia tier with materially higher rate limits) would remove the root cause rather than paper over it with retries — retries help but don't raise the ceiling. Left as a config change for the user to make when convenient, not made unilaterally here.
- Verified: `mintCatalogueOnchain` re-run directly against the live contract after the fix succeeded (not under rate limit at the time, so this exercises the happy path through the new wrapper, not the retry branch itself — the retry branch's correctness rests on the transient nature already confirmed by the immediate-retry reproduction above). Checked production's `GET /api/data` against `GET /api/onchain/list` afterward: every currently-persisted asset is already chain-verified, so no backfill was needed for existing data — the gap only affected the specific creation attempt(s) that hit the rate limit and were never retried by hand.

**2.23 Campaign data is now durably stored — migrated off Render's ephemeral disk to Turso (30 Aug 2026).** Concrete proof the gap flagged in §2.20/§2.22 was real, not theoretical: the user created a track ("Guns") right when the mint-retry bug (§2.22) was being diagnosed; the mint failed as expected, but three subsequent pushes to fix unrelated bugs each triggered a Render redeploy, and each one wiped the local SQLite file — so by the time the retry fix landed, the campaign's own metadata (title, description, everything not on-chain) was already gone, unrecoverable. The user's instruction was unambiguous: campaigns must be permanent. Two options were on the table — pay for a Render persistent disk (~$7.25/mo, zero code changes, still just one un-replicated SQLite file) or migrate to a real external database. The user picked migration, reasoning it's the actually production-appropriate choice, not a stopgap.
- **`server/db.js`** (new) — thin async wrapper around `@libsql/client`, exposing the same shape the code already used (`db.prepare(sql).get/all/run(...)`) so every call site only needed `await` added, not restructured. Picks its backend from env: `TURSO_DATABASE_URL` + `TURSO_AUTH_TOKEN` set → talks to a real network-hosted Turso (libSQL) database; neither set → falls back to a local file (`file:<DB_PATH>`, unchanged default `server/humfiverse.db`) — so local development needs no Turso account at all, only production does.
- **`server.js`** rewritten to the async DB API throughout (`node:sqlite`'s `DatabaseSync` — synchronous — is gone entirely, along with the `--experimental-sqlite` flag it needed): `getAssets`, `getCampaigns`, `getPortfolio`, `redeem`, `recordContractAcceptance`, `recordKyc`, `getOnchainRecord`, `mintAssetOnchain`, `createEscrowCampaign` and every route handler that touches storage are now `async`/`await`. Schema creation (`initSchema`) and seeding (`seedIfEmpty`) now run once at startup before `server.listen`, instead of synchronously at module load.
- **Verified the actual failure mode is fixed**, not just that the code runs: created a test asset against the real Turso database, killed the Node process entirely, started a fresh one, and confirmed the asset was still there — this is exactly what a Render redeploy does to the old local-file setup, and exactly what used to lose data (§2.18, and concretely, "Guns").
- Real database created: `humfiverse` on Turso's free tier (`aws-eu-west-1`), via `turso db create humfiverse` — no cost, no expiry, no credit card. `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` need to be set in Render's environment group for production to pick this up (same manual step as `ADMIN_API_KEY`/`CHAIN_OPERATOR_PRIVATE_KEY` before it) — until that's done, production keeps running on the old ephemeral local file.
- **Still local-only after this fix**: `contract_acceptances` and `kyc_records` now live in Turso too (same tables, same migration), so they're durable as a side effect — but neither has an on-chain equivalent the way tokens/escrow do, so Turso is now their only copy, full stop, not a cache in front of a chain source of truth.

**2.24 Track title, artist, and studio name are now readable straight off the deployed contracts — plus real Basescan verification (1 Sep 2026).** The user asked how to make the data registered through the site visible on the block explorer, then, once the distinction was explained, explicitly asked for track title, artist, and studio name specifically to be on-chain-readable. Two separate mechanisms were involved:
- **Contract verification**: Sourcify (attempted in §2.17) is still broken upstream (404 on its `check-all-by-addresses` endpoint) — worked around this time with Basescan's own native verification instead, using the unified Etherscan V2 API (one API key, works across all their supported explorers including Base Sepolia — no separate Basescan account needed). `hardhat.config.js`'s `etherscan.apiKey` now reads `ETHERSCAN_API_KEY` from `.env`. Both `HumfiverseCatalogueToken` and `HumfiverseMilestoneEscrow` are now verified — anyone can read their real source, and Basescan decodes every function call and event log using it instead of showing raw hex.
- **What's actually on-chain, extended**: `HumfiverseMilestoneEscrow`'s `Studio` struct already stored the studio's name (since §2.15) — nothing to add there. `HumfiverseCatalogueToken` did not store title/artist at all; `mintCatalogue` gained two new `string` params (`title`, `artist`), two new public mappings (`trackTitle`, `artistName`), and the `CatalogueMinted` event now carries both — all readable directly via contract calls or decoded event logs on a verified explorer page, not only from the off-chain database. 1 new assertion added to the existing mint test (18 total for this contract, 56 across all three).
- **Redeployed** (bytecode change, not upgradeable in place, same pattern as every prior contract change in this doc) to [`0x71FcCA4a27e251377A53Cda8A5c2D8494Fc85cba`](https://sepolia.basescan.org/address/0x71FcCA4a27e251377A53Cda8A5c2D8494Fc85cba#code), superseding §2.13's `0xFa9CCBCAAbd...e99c2`. All 6 currently-existing catalogue tokens re-minted with real title/artist: the 4 demo seeds, plus — significant — two tracks the user had genuinely created through the wizard since the §2.23 persistence fix (`guns-448` "Guns" by Gunner, `black-sail-739` "Black Sail" by Victor), which would otherwise have been orphaned by this redeploy (correctly minted on the old contract, but the old contract is no longer what the app reads from). Re-minted onto the new contract at their same token ids, and the Turso `onchain_tokens` cache rows updated in place with the new tx hashes so the cache doesn't keep pointing at the superseded contract's transactions.
- **`server/chain.js`**: ABI/address/deploy-block updated; `getPoolInfo` now also reads `trackTitle`/`artistName` and returns them as `onchainTitle`/`onchainArtist`; `mintCatalogueOnchain` and `listMintedSlugsFromChain` pass/parse the two new fields. `server.js`'s `mintAssetOnchain` and `POST /api/onchain/mint` now accept and forward `title`/`artist`. `onboarding.component.ts`'s mint call now sends `asset.title`/`asset.artistName`, so every future mint — not just this backfill — writes them on-chain automatically. Asset Detail's on-chain panel gained a line showing the title/artist as read back from the contract itself (`detail.onchainRecordNote`, all 9 locales) — a small proof-of-provenance, not a new data source for the rest of the UI, which still reads from the backend for everything else.

**2.25 The 6 fictional demo catalogues are gone from the site — real user-created data only, from now on (1 Sep 2026).** With real persistence (§2.23) and real on-chain title/artist (§2.24) in place, the user made an explicit call: the site should now only show data that genuinely came from someone using it, not placeholder content written into the seed data at the start of this project — even though that placeholder content did have a real, verified on-chain token. "Fake, even if minted" and "real, because a user made it" are the operative categories now, not on-chain-or-not (which was §2.14's distinction, still correct as far as it goes, just not sufficient here).
- `seedIfEmpty()` in `server.js` is now an intentional no-op (kept as a documented hook, not deleted, in case a future non-production/demo deployment wants seed data back — `seed-data.js` is untouched for that). A freshly empty database now starts genuinely empty — `GET /api/data` returns `{assets: [], campaigns: [], portfolio: {holdings: [], distributions: []}}` until real campaigns exist — rather than auto-populating 6 fictional tracks, their mock portfolio holdings, and their onchain_tokens cache rows.
- The 6 pre-existing demo rows (`midnight-static`, `ember-choir`, `paper-cranes`, `copper-radio`, `glass-horizon`, `low-tide-orchestra`) and their mock portfolio holdings/distributions were deleted from the production Turso database directly — the two genuinely user-created tracks (`guns-448`, `black-sail-739`) were left untouched. Their on-chain tokens are unaffected and permanent (nothing on Base Sepolia can or should be deleted) — they're simply no longer surfaced by this site, which is exactly the intended distinction: on-chain permanence for what's real, off-chain visibility control for what's shown.

**2.26 Bug found and fixed: two campaigns sharing a studio wallet silently shared one studio name too (1 Sep 2026).** The user noticed Black Sail's on-chain studio name was showing as Guns' studio name ("Peppe o bio") instead of the one actually entered for Black Sail ("black and raimbow label"). Root cause: both campaigns had used the same wallet address for their studio, and `createEscrowCampaign` in `server.js` deduplicated purely by wallet (`SELECT studio_id FROM escrow_studios WHERE wallet = ?`) — whichever name was registered *first* for a wallet silently won on every later campaign reusing it, since the code skipped calling `registerStudioOnchain` again and just reused the cached `studioId`, name mismatch or not.
- **Fix**: the lookup now matches on wallet *and* name (`WHERE wallet = ? AND name = ?`) — a repeated wallet only skips re-registration when the name genuinely repeats too; a new name for an already-seen wallet now correctly registers a fresh on-chain studio. The cache write changed from `INSERT` to `INSERT OR REPLACE` since `wallet` is still `UNIQUE` in the local cache table (unchanged schema — this table is a gas-saving cache, not a correctness dependency, so it only needs to remember the *most recent* wallet→name pairing, not every historical one).
- **The already-broken on-chain record couldn't be silently corrected** — `HumfiverseMilestoneEscrow` had no way to rename an already-registered studio, and `campaignIdByAssetId` permanently locks an asset to its first campaign (not even `cancelCampaign` clears it), so Black Sail's specific campaign could never get a different `studioId` on the same contract. Given the choice between leaving this one historical record wrong or doing it properly, the user chose the real fix: added `renameStudio(uint256 studioId, string name)` (owner-only, emits `StudioRenamed`) to the contract for future corrections, and — since adding it meant a redeploy anyway — took the opportunity to also cleanly migrate the two genuinely real campaigns onto the new contract with correct, separate studios from the start, rather than relying on the new rename function for a same-studioId workaround that would've been semantically wrong anyway (Guns and Black Sail really do have two different studios that happen to share a wallet in this test data — they deserve two distinct `studioId`s, not one shared, renamed one).
- **Migration, safe because nothing had been contributed yet** (`raised: 0` on both campaigns, confirmed before touching anything): redeployed to [`0xAa604CB2A0A3D22d2382Eb0685774f284735873f`](https://sepolia.basescan.org/address/0xAa604CB2A0A3D22d2382Eb0685774f284735873f#code) (superseding §2.19's `0xbc220eB...4C1a1`), verified on Basescan. Registered two fresh studios (`studioId` 1 = "Peppe o bio" for Guns, `studioId` 2 = "black and raimbow label" for Black Sail) and recreated both campaigns with their original funding goals and milestone structure. 3 new tests (58 total across all three contracts): rename updates the name and every campaign sharing that studioId sees it, owner-only, reverts on an unknown studioId.
- Same public-RPC read-lag pattern noted repeatedly in this doc (§2.13, §2.22) showed up again mid-migration — `createCampaign` reverted with "studio not active" seconds after a successful `registerStudio` confirmation, because the read hit a lagging RPC node that hadn't caught up yet; waiting a few seconds and retrying succeeded. Not a new failure mode, just another instance of the same one — `chainRetry.js`'s retry wrapper (§2.22) doesn't cover this specific case since a revert isn't classified as transient, only genuine RPC-level errors are; a real fix would need read-your-writes consistency from the RPC provider, not a client-side workaround.

**2.27 Humfiverse removed its own power to confirm milestones — release now requires the artist and the studio to agree, on-chain, with no arbitration (1 Sep 2026).** Direct follow-up to a real gap surfaced while discussing legal-regulatory-notes.md §7.3 with the user: the AIFMD-avoidance argument for the Luxembourg vehicle rests on it not being "actively managed... per a discretionary investment policy" (§7.3/§7.7), but the escrow contract's `confirmMilestone` gave Humfiverse sole, unreviewable discretion over exactly the thing that controls fund release — a real, previously unflagged tension between two parts of the same research. The user's own resolution: build the milestones so Humfiverse can only attest that production happened and that the artist and studio agree on what was delivered — not decide the merits itself. If they disagree, funds simply stay locked; no arbitration or timeout, on purpose.
- **Contract redesign**: `confirmMilestone(campaignId, milestoneIndex) onlyOwner` is gone entirely — replaced by `confirmMilestoneAsArtist`/`confirmMilestoneAsStudio`, each callable only by that campaign's own artist/studio wallet. A private `_tryRelease` pays out a tranche only once both `artistConfirmed[campaignId][index]` and `studioConfirmed[campaignId][index]` are true (a campaign with no studio only needs the artist) — checked and executed automatically on whichever confirmation comes second, or immediately if the funding threshold is met later. This applies to **every** milestone, not just the studio-payee one — the point is agreement on what was actually produced, not just who happens to get paid for a given tranche. There is deliberately no public function anywhere in the contract, Humfiverse's owner role included, that can release a tranche on a single party's say-so.
- **Redeployed** to [`0x447c8F2b3039497E7BFc6C16B715EBf5DB4E3F35`](https://sepolia.basescan.org/address/0x447c8F2b3039497E7BFc6C16B715EBf5DB4E3F35#code) (superseding §2.26's `0xAa604CB2...5873f`), verified on Basescan. 61 tests total (up from 58) — dual-confirmation release, no-release-on-one-sided-confirmation, no-release-under-funding-threshold-even-with-both-confirmed, and an explicit assertion that `confirmMilestone` no longer exists on the ABI at all. Migrated the two real campaigns (Guns, Black Sail — both still unfunded) onto the new contract with fresh, correctly-named studios, same pattern as §2.26.
- **Backend**: `chainEscrow.js` no longer exposes any function that writes a milestone confirmation — `getCampaignInfo` now also reads and returns each milestone's `artistConfirmed`/`studioConfirmed` state, purely observational. `POST /api/escrow/confirm` now returns `410 Gone` with an explanation instead of calling a function that no longer exists, rather than leaving a dead route to fail confusingly.
- **`/admin/escrow` is now read-only** — the confirm button and its `ADMIN_API_KEY` prompt are gone (that key still gates `DELETE /api/assets/:id`, unaffected); the page shows each milestone's status as "waiting on artist" / "waiting on studio" / "waiting on both" / "both confirmed — releasing" / "released," which is now the only thing left for Humfiverse to observe here.
- **`WalletService` gained `confirmMilestoneAsArtist`/`confirmMilestoneAsStudio`**, mirroring `buyOnchain`/`contributeOnchain`'s pattern of requesting a real signature from the actual counterparty rather than the operator key. **No UI surface calls them yet** — there's no existing "artist's campaigns" or "studio" account concept anywhere in this app to hang a confirm button off of. Building that (an artist dashboard view per campaign, and some way for a studio to even reach the app at all — it has no login concept today) is a real, separate product question, flagged here rather than built into this same change.

**2.28 Built the UI surface §2.27 flagged as missing: a studio page and an artist milestones page (1 Sep 2026).** §2.27 shipped `confirmMilestoneAsArtist`/`confirmMilestoneAsStudio` on-chain and in `WalletService`, but nothing in the app could actually call them — no studio account concept existed at all, and the artist dashboard didn't surface milestone confirmation. The user asked for both directly.
- **`/studio`** (new, linked from the footer as "For studios" — no natural home in the existing artist/investor perspective switch, so it gets its own always-visible entry point) and **`/artist/milestones`** (new, added to the artist-perspective nav alongside the existing dashboard/onboarding links). Near-identical wallet-gated components: connect a wallet, fetch every escrow campaign via `GET /api/escrow/campaigns`, filter client-side to the ones where the connected address matches (`studio.wallet` for the studio page, `artist` for the artist page), and show each milestone's status — released, not-enough-raised-yet, or which side is still waited on — with a confirm button that calls the matching `WalletService` method when the milestone is actually confirmable.
- **This *is* the studio's "onboarding"**, and it's worth stating plainly why there isn't more to it: a studio isn't self-signed-up anywhere in this system — an artist registers one by entering a name and wallet address when they create a preproduction campaign (§2.16). `/studio` has nothing to sign up *for*; connecting the matching wallet is the whole flow, and the page says so directly when a connected wallet doesn't match any registered studio, rather than presenting a form that would misleadingly imply self-registration exists.
- **Found and fixed a real pre-existing bug while wiring this up**: `GET /api/escrow/campaigns` never actually included an `escrow: true` field on each entry, even though both this route's own consumers (the old admin panel, and now these two new pages) filtered on exactly that field being `true`. Every campaign was silently dropped by the filter — meaning `/admin/escrow`'s campaign list has likely been empty for every visit since it shipped in §2.16, not just since §2.27's redesign. One-line fix in `server.js`; caught by testing the new pages end-to-end with a fake injected wallet provider before shipping, not by inspecting the old code path directly.
- **Also fixed while building**: both new components only called their initial data load from an explicit "Connect wallet" button click handler — meaning a visitor whose wallet was *already* connected (silently synced by `WalletService.silentSync()` at app bootstrap) would see neither a connect button nor any data, a dead page. Fixed with an `effect()` that loads whenever an address becomes present, regardless of how it got there.
- i18n shipped in all 9 locales (30 new keys — page copy plus shared `escrowStatus.*` phrasing reused by both pages, worded from each viewer's own perspective — "waiting on you" vs. "waiting on the artist/studio" — rather than a single neutral status string).

**2.29 Bug found and fixed: the on-chain pool balance shown in the UI didn't update after a real purchase (1 Sep 2026).** The user caught this directly: after a real, signed `buyOnchain()`/`contributeOnchain()` transaction, Asset Detail's "On-chain token" panel kept showing the pre-purchase pool balance/raised amount until the page was fully reloaded. Root cause: `onchainInfo`/`escrowInfo` were only ever fetched once, by an `effect()` keyed on the asset id — a successful purchase called `applyPurchase()` (which updates the local *mock* `tokensSold` figure only) but never re-fetched the *real* on-chain read that feeds that same panel, so the two numbers silently diverged after every real transaction.
- Fixed by extracting the fetch into `refreshOnchainState(id)` and calling it again — not just at first load — right after a successful `buyOnchain`/`contributeOnchain` call, in both branches of `buy()`.
- Called twice: immediately, and again after a 4s delay via `scheduleOnchainRefresh`. The immediate call alone would sometimes still show stale data — the same public-RPC read-lag pattern noted repeatedly in this doc (§2.13, §2.22, §2.26) means a read issued right after a write can hit a node that hasn't caught up yet. Not a guarantee (still no polling loop, no retry-until-changed), but enough to self-correct within a few seconds rather than requiring a manual reload.

**2.30 KYC/appropriateness is now remembered per wallet, not re-required every session (1 Sep 2026).** The user's ask, correctly framed as a bug: MiFID appropriateness verification should be asked of a genuinely new wallet, not repeated for one that's already completed it. It turned out there was no mechanism for this at all — `kyc_records` never stored a wallet address, and `store.investor().verified` was a plain in-memory signal reset on every reload, completely disconnected from wallet identity. Every visit re-required KYC regardless of who was connecting, which is also arguably worse than the fix: an appropriateness assessment tied to nothing durable isn't really assessing anything durable either.
- **`server/db.js` schema**: `kyc_records` gained `wallet_address` (migrated in for an already-existing table via a best-effort `ALTER TABLE ADD COLUMN`, tolerating the "already exists" case — `CREATE TABLE IF NOT EXISTS` alone doesn't touch a table that's already there). `recordKyc` now stores it (lowercased); new `getKycStatusForWallet(wallet)` looks up the most recent record for that address. New route `GET /api/kyc/status/:wallet`.
- **`StoreService.syncKycForWallet(address)`** (new): on a verified hit, marks `investor` verified with the stored classification/score/receipt; on no hit (or `address === null`, i.e. disconnected), resets to unverified — deliberately not sticky, so a *different*, never-verified wallet doesn't inherit a previous wallet's verification in the same browser tab. Wired from `app.ts` via an `effect()` that fires whenever `WalletService`'s connected address changes (covers both an explicit connect and `silentSync()`'s auto-detection on page load).
- **`KycComponent.submit()`** now sends the connected wallet's address along with the form, so a completed assessment has something durable to attach to.
- **Verified end-to-end** with a fake injected wallet provider: completed KYC once, then loaded the asset page fresh (new browser context, same wallet) — buy panel appeared immediately, no re-prompt. A second, never-verified wallet against the same asset still correctly showed the KYC-required gate. No backend/contract change beyond the schema migration — purely an application-layer fix.

**2.31 Bug found and fixed: "tokens remaining" didn't update after funding a preproduction campaign (1 Sep 2026).** The user caught it live: clicking "Fund this campaign" (and, it turned out, buying a catalogue token too — same code path) didn't move the remaining-tokens figure on screen. Root cause: `applyPurchase()` in `asset-detail.component.ts` did `a.tokensSold += qty` — a direct, in-place mutation of an object already sitting inside the `store.assets` signal's array, without ever calling `.set()`/`.update()` on that signal. Angular's signal-based reactivity only re-renders when a signal itself is reassigned; mutating a nested property on an object it already holds is invisible to anything reading `store.assets()` (including the `computed()` that resolves this very asset), so the number on screen simply never picked up the change — it would only ever show correctly after something *unrelated* forced a re-render and happened to re-read the same (mutated) object.
- Fixed by routing the update through the signal properly: `this.store.assets.update((assets) => assets.map((x) => (x.id === a.id ? { ...x, tokensSold: x.tokensSold + qty } : x)))`.
- Checked for the same mistake elsewhere (`grep` for direct `.tokensSold`/`.tokens` mutation across the app) — `portfolio.component.ts`'s holding mutations looked structurally similar but are actually fine: they mutate a nested object *inside* a `store.portfolio.update()` callback that still returns a new outer object, so the portfolio signal itself does get reassigned and reactivity fires correctly. This bug was specific to `asset-detail.component.ts` mutating a different signal (`assets`) than the one it was updating (`portfolio`).
- Verified end-to-end (no wallet, simulated purchase path — the bug wasn't chain-specific, so a real on-chain refresh wasn't needed to reproduce or confirm it): remaining count read 100, dropped to 99 immediately after funding, no reload needed.

**2.32 "Remaining" and funding % now come from the chain, not a mock counter that's never persisted (1 Sep 2026).** Direct follow-up to §2.31: the user reported the same bug persisting even after that fix. It genuinely was fixed for the same-page, no-reload case (verified live on production: 1,300 → 1,299 immediately after funding) — but on a fresh page load, the figure reverted to its pre-purchase value, because `tokensSold` was never sent to the backend at all; §2.31 only fixed *reactivity within a session*, not *durability across one*. The deeper issue: `remaining()`/`fundingPct()` were computed purely from `Asset.tokensSold`/`tokensTotal`, a mock field disconnected from the real on-chain state this app has spent the last several sections making authoritative for everything else.
- `remaining(a)` and `fundingPct(a)` in `asset-detail.component.ts` now prefer real on-chain reads when available: `onchainInfo().poolBalance`/`totalSupply` for a catalogue token, `escrowInfo().raised`/`fundingGoal` for a preproduction campaign — both already being fetched and, since §2.29, re-fetched right after a real purchase. Falls back to the mock `tokensTotal - tokensSold` only when no on-chain data exists (e.g. this asset was never minted, or the chain read failed) — same graceful-degradation pattern used throughout the backend.
- BigInt arithmetic kept until the final step in both methods, specifically to avoid `Number` precision loss converting large wei amounts (funding goals here run ~1e18, well past `Number.MAX_SAFE_INTEGER`) — the same pattern `weiToUsd()` already used elsewhere in this file.
- **Verified with a genuinely real, signed contribution** (not simulated): sent 0.00001 ETH to `guns-448`'s live escrow campaign directly via the operator wallet, confirmed `raised` moved on `GET /api/escrow/campaign/guns-448`, then loaded the asset page completely fresh (new browser context, no prior interaction) and confirmed "remaining" reflected the real contribution without any client-side purchase flow having run in that session at all — durable specifically because it's re-read from the contract every load, not because anything about the purchase itself was cached or persisted client-side.

**2.33 One more spot on the same page still read the mock counter (1 Sep 2026).** The user pointed at Black Sail specifically: the "X/Y tokens" stat tile (between token price and use-of-funds) still showed the pre-purchase count even after §2.32. It was a second, separate template binding — `a.tokensSold.toLocaleString()` — that §2.32 hadn't touched, since that fix only updated `remaining()`/`fundingPct()`, not this tile's own direct read of the same mock field. Added `tokensSold(a)` as `a.tokensTotal - this.remaining(a)` — the complement of the already-fixed chain-aware `remaining()`, so it inherits the same on-chain-when-available behavior for free — and pointed the template at it. Checked the rest of the app for the same raw `a.tokensSold` read: `format.util.ts`'s `fundingRaised()`/`fundingPct()` (used by the marketplace grid and artist dashboard aggregates) still use it — left as-is deliberately, since making every listed asset in a grid chain-aware would mean fetching on-chain/escrow state for every card, not just the one asset a detail page already loads; scoped this fix to where the user actually encountered it.

**2.34 A contribution now actually releases the matching tokens — and a CSS bug fixed along the way (1 Sep 2026).** Two more asks in the same message. First: why does the "On-chain token" pool/supply panel never move for Black Sail even after the §2.32/§2.33 fixes? Because `HumfiverseMilestoneEscrow.contribute()` and `HumfiverseCatalogueToken` are entirely independent contracts with no cross-calls — contributing to escrow was never wired to release anything from the token pool at all. Offered the user a choice between documenting this as expected (cheap) or actually releasing real tokens per contribution (a real feature, with a flagged tradeoff around cancelled-campaign refunds); the user chose the real fix.
- **`chain.js`**: added `releaseFromPool` to the ABI and a new `releaseFromPoolOnchain(tokenId, to, amount)`.
- **`chainEscrow.js`**: added the `Contributed` event to the ABI (it was never there — nothing had needed to decode it before) and `getContributionFromTx(txHash)`, which reads a transaction receipt and confirms it really did emit that event, returning `{campaignId, contributor, amountWei}`.
- **`server.js`**: new `releaseForContribution(assetId, contributionTxHash)` — verifies the tx via `getContributionFromTx`, cross-checks its `campaignId` against `escrow.getCampaignInfoByAssetId(assetId)` (so a client can't claim an unrelated contribution belongs to a different asset), reads the token's own on-chain `priceWeiPerToken` and computes `qty = floor(amountWei / priceWei)` in BigInt (no precision loss), then calls `releaseFromPoolOnchain`. Idempotent via a new `onchain_releases(contribution_tx_hash PRIMARY KEY, asset_id, ...)` table — a repeat call for the same tx returns the cached result instead of re-releasing. New route `POST /api/onchain/release-for-contribution`. Also extracted `getOnchainRecordWithFallback()` (the chain-scan-and-reseed logic `GET /api/onchain/:assetId` already had) so this new function benefits from the same cache-miss resilience instead of duplicating or lacking it.
- **A real bug caught while testing this same endpoint**: the idempotency check matched on `contribution_tx_hash` alone, so a second call for the *same* tx but a *different* (wrong) `assetId` incorrectly returned the first call's cached result instead of being rejected as a mismatch. Fixed by also comparing `asset_id` before treating a hit as "already processed."
- **Frontend**: `asset-detail.component.ts`'s real-contribution branch now calls `ApiService.releaseForContribution()` best-effort right after `contributeOnchain()` succeeds, then triggers another `scheduleOnchainRefresh()` once the release itself confirms (the existing immediate+4s refresh from §2.29 fires too early to see it).
- **Second ask, unrelated bug, same message**: the "wallet connected — real transaction ready" status was rendered in a `.chip` (a pill built for short labels, `white-space: nowrap`) but the actual copy is a full sentence — it visibly overflowed the narrow buy-panel sidebar. Same exact bug existed in the onboarding wizard's review step for the escrow-ready message. Fixed both by switching to `.risk-item` (already used for the equivalent "connect your wallet" state right next to it, which wraps correctly) with a new `.risk-item.good` green variant.
- **Verified**: `releaseForContribution` tested against the local backend with a chain-cold cache (fallback path exercised), a real (small, sub-1-token) contribution tx correctly computed and recorded `qty: 0` without wasting gas on a no-op release, a repeat call returned the cached result, and a wrong-assetId call now correctly rejects instead of returning stale data. A larger contribution to actually see `qty > 0` release live wasn't possible this session — the operator wallet's Base Sepolia balance is down to ~0.00001 ETH and needs a testnet faucet top-up.

**2.35 Switched networks: Base Sepolia → real Ethereum Sepolia (1 Sep 2026).** User's reason, entirely practical: Sepolia's own testnet-ETH faucets are far easier to get funds from than Base Sepolia's, and this session had repeatedly been slowed down by an underfunded operator wallet. Offered the alternative first — bridging Sepolia ETH to Base Sepolia via bridge.base.org, which would have needed zero code changes and kept every existing deployed contract and on-chain record valid — but the user explicitly chose the full migration instead.
- **Everything with a Base-Sepolia-specific value got touched**: `contracts/hardhat.config.js`'s network entry (chainId 84532→11155111, renamed `baseSepolia`→`sepolia`, RPC default→`https://ethereum-sepolia-rpc.publicnode.com`), `contracts/package.json` script names, both `.env`/`.env.example` files (`BASE_SEPOLIA_RPC_URL`→`SEPOLIA_RPC_URL`), `server/chain.js` and `chainEscrow.js` (`CHAIN_ID`, RPC default, `EXPLORER_BASE`→`sepolia.etherscan.io`, hardcoded `"network": "base-sepolia"` response fields), `webapp/src/app/core/wallet.service.ts` (renamed `ensureBaseSepolia()`→`ensureSepolia()`, new chain-ID-hex/add-chain-params constants, 4 hardcoded `sepolia.basescan.org` explorer-URL string templates), and 3 i18n keys (`toast.onchainMinted`, `detail.onchainNetwork`, `toast.onchainWrongNetwork`) across all 9 locales that named "Base Sepolia" directly in their copy.
- **Redeployed both contracts** (bytecode-identical, just a new chain — same pattern as every prior redeploy in this doc) to Sepolia: [`HumfiverseCatalogueToken` at `0xC1aFD3D24de2C344053bBe83aB412140C452146b`](https://sepolia.etherscan.io/address/0xC1aFD3D24de2C344053bBe83aB412140C452146b#code) and [`HumfiverseMilestoneEscrow` at `0x3Bac66168748798ec174BDEd855376c4e7012Dba`](https://sepolia.etherscan.io/address/0x3Bac66168748798ec174BDEd855376c4e7012Dba#code), both verified via the same Etherscan V2 unified API key already in use (§2.24) — Sepolia needed no new account or key, just the existing one pointed at a different chain id.
- **Recreated the real data that existed on Base Sepolia**, since none of it carries over to a different chain: re-minted Guns and Black Sail's tokens with their real title/artist, re-registered both studios, and re-created both escrow campaigns with their original funding goals and milestone structure — matching exactly what §2.24/§2.26's contract-upgrade migrations already did for a same-chain redeploy, just now also across a chain switch. The two small real contributions made earlier this session (§2.32, §2.34's verification) stay behind on Base Sepolia, unrecoverable from Sepolia — acceptable, since they were test transactions with no real value at stake.
- Cleared and reseeded the Turso `onchain_tokens`/`escrow_campaigns`/`escrow_studios` cache rows to match the new chain's token/campaign ids, and cleared `onchain_releases` (§2.34) since it was keyed by Base-Sepolia-only transaction hashes.
- **Production env vars on Render** (`CHAIN_RPC_URL`, `CHAIN_CONTRACT_ADDRESS`, `CHAIN_ESCROW_ADDRESS`) needed a manual update — same pattern as every earlier contract-address change this session; the code defaults are also updated so a fresh environment without those explicitly set still points at the right chain and contracts.

**2.36 Wallets could never actually display a token's name/image/balance — the metadata URI pointed at a domain that never resolves (1 Sep 2026).** User noticed, after importing a token into MetaMask, that the detail card was missing basic fields (including balance) — asked how to fix that specifically once it became clear it wasn't just a MetaMask refresh quirk. Root cause: `HumfiverseCatalogueToken`'s constructor set its ERC-1155 base URI to `https://humfiverse.example/...` — `.example` is an IANA-reserved TLD (RFC 2606) that is *guaranteed* to never resolve, used here as a placeholder that nobody had gone back to replace once the app had a real backend to point at. Every wallet's metadata fetch for every token had been failing silently since the very first mint.
- **Contract**: added an owner-only `setURI(string)` (wrapping OpenZeppelin's internal `_setURI`) so this class of mistake is correctable in the future without a full redeploy — then, for this redeploy itself, fixed the constructor's default straight to the real endpoint rather than deploying-then-calling-setURI. 2 new tests (63 total across all three contracts).
- **Backend**: new `GET /api/token-metadata/:id.json` (the 64-hex-char, zero-padded, lowercase id format EIP-1155 itself specifies wallets substitute into `{id}` — not this project's own convention) returns real ERC-1155-standard JSON (`name`, `description`, `image`, `attributes`) read live from `chain.getPoolInfo()` — title/artist/supply, all already on-chain since §2.24. New `GET /api/token-metadata/:id/image.svg` generates a small, deterministic, on-brand card (same purple/cream palette as the site) server-side — no artwork to source per track, no external image host dependency.
- **Redeployed** `HumfiverseCatalogueToken` (bytecode change) to [`0x51090e5767F67aFc52725B2267f409811d59d03d`](https://sepolia.etherscan.io/address/0x51090e5767F67aFc52725B2267f409811d59d03d#code) on Sepolia, verified. Re-minted Guns and Black Sail. **`HumfiverseMilestoneEscrow` did not need touching** — the two contracts have never called each other (confirmed repeatedly this session, most recently in §2.34), so both escrow campaigns and their studios stayed exactly as §2.35 left them; only `onchain_tokens`' cache rows needed updating.
- Verified end-to-end: read `uri(1)` directly off the deployed contract and confirmed it returns the real endpoint URL; fetched that URL's `{id}`-substituted form locally and got back correct JSON with a working `image` link.

**2.37 Portfolio now shows real on-chain holdings, not fictional seeded data (1 Sep 2026).** Follow-up to the wallet-balance troubleshooting in this same conversation: the user asked for the Portfolio page itself to be rebuilt on real data — it still showed `Portfolio.holdings`, mock JSON seeded at app start referencing demo assets that no longer even exist (removed in §2.25), with a "cost basis"/"unclaimed royalties"/"redeem" flow that was never connected to any real wallet at all.
- **Backend**: `chain.js` gained `getBalance(tokenId, address)` (a plain `balanceOf` read). New `GET /api/portfolio/:wallet` reads every known minted token from the local table and returns real balances for that address — small, cheap for a testnet contract this young. (This route's data source was reworked again in §2.39.)
- **`PortfolioComponent` rewritten** around this: holdings are now fetched live whenever the connected wallet changes (same reactive-`effect()` pattern as `StudioComponent`/`ArtistMilestonesComponent`/app.ts's KYC sync), replacing `store.portfolio()` as the page's data source entirely. "Cost basis" became "Value" (tokens × the token's real on-chain price — this app still has no purchase-price indexer, so a true historical cost basis isn't something it can honestly show); "unclaimed royalties," the redeem flow, and distribution history were removed outright rather than kept showing permanently-zero numbers, since there's no real royalty oracle behind them (consistent with how the rest of the app treats royalty distribution as still-simulated, §2 throughout, and with the wizard-flow diagram artifact's own phase 5 framing earlier this session). The resale/listing feature stays — still a client-side simulation (`HumfiverseMarketplace.sol` isn't deployed, §2.11/§2.12) — now operating on real token counts instead of fictional ones.
- **A real accounting mistake surfaced and fixed while diagnosing the underlying "why is my balance wrong" question that led here**: two of the user's real Sepolia contributions to Guns' escrow campaign were made (and auto-released, §2.34) *before* §2.36's metadata-fix redeploy propagated, landing their tokens on the now-superseded contract; a third contribution landed correctly on the new one. Manually re-released the first two's worth to the new contract to make the wallet whole — but did the arithmetic against stale information and released one token too many on the first pass, caught and corrected by re-deriving the real total directly from each contribution's own on-chain event rather than trusting a partially-overwritten local cache. Net effect on the real chain: the wallet holds exactly what 4 real contributions paid for, no more, no less.
- **Caught and fixed a real bug while testing the new endpoint**: a malformed wallet address wasn't rejected before being handed to `ethers`, which then tried to resolve it as an ENS name and surfaced an opaque `ResolverNotFound` 502 instead of a clear error. Added a basic `0x` + 40-hex-char format check, returning 400 for anything else.
- i18n shipped in all 9 locales.

**2.38 Bug found and fixed: translated strings with HTML markup rendering as literal text (3 Sep 2026).** The user pasted exactly what the empty-holdings state showed — including a literal `<a href="#/marketplace">...</a>` tag as visible text instead of a rendered link. Root cause: `portfolio.emptyBodyReal`'s translation string contains real HTML (the marketplace link), but the template bound it with plain `{{ }}` interpolation, which Angular escapes for safety rather than parses as markup — only `[innerHTML]` renders it. Same bug existed in `asset-detail.component.html`'s purchase-success message (`success.body`, which uses `<strong>` tags around the quantity/title), untouched by whatever earlier pass fixed the other `&amp;`-entity instances of this same class of bug. Both fixed by switching to `[innerHTML]`. Checked every i18n key containing HTML tags against its template binding to confirm no other instances remain.

**2.39 The marketplace and Portfolio silently went empty in production — a free-tier RPC's `eth_getLogs` unreliability, and the real fix required an architectural change, not just a better provider (3 Sep 2026).** While re-testing the Portfolio page after §2.37/§2.38, the user's real wallet (holding 12 real tokens, confirmed by direct `balanceOf` reads) showed no holdings at all. Investigating traced it to `ethereum-sepolia-rpc.publicnode.com` — the public RPC this project defaulted to since §2.35's network migration — silently returning an *incomplete* result for `eth_getLogs` queries spanning the contract's full deploy-to-latest block range, instead of erroring. This is worse than an outright failure: the existing code (`listMintedSlugsFromChain`, used by `/api/onchain/list`, `/api/portfolio/:wallet`, and `chainEscrow.js`'s equivalent `listCampaignAssetIdsFromChain`) treated any non-null scan result as authoritative, so a silently-truncated scan made the marketplace itself report zero assets in production — a regression well beyond the Portfolio bug the user had actually flagged.
- **Provider comparison, tested directly**: `rpc.sepolia.org` — dead, 404. `rpc.ankr.com/eth_sepolia` — hangs indefinitely. `ethereum-sepolia-rpc.publicnode.com` — silently incomplete on a full-history range, as above. Switched to Alchemy (user's choice) using a real, guided walkthrough to get the endpoint.
- **Alchemy's free tier turned out to have its own hard limit** — `eth_getLogs` capped at a 10-block range per call (`"Under the Free tier plan, you can make eth_getLogs requests with up to a 10 block range"`), discovered by direct testing. This made the original design — scan every block from the contract's deploy block to latest, previously chunked at 9,000 blocks per call — impractical on *any* free-tier provider (roughly 1,780 calls needed for this contract's ~16,000-block history), not just this one.
- **Architectural fix, applied to both `chain.js` and `chainEscrow.js`**: the local SQLite/Turso table (`onchain_tokens`, `escrow_campaigns` — both already self-healing caches, §2.18/§2.20) is now the *primary* source for listing, not a fallback used only when a chain scan fails. `listMintedSlugsFromChain`/`listCampaignAssetIdsFromChain` were renamed to `listRecentlyMintedSlugsFromChain`/`listRecentlyCreatedCampaignAssetIdsFromChain` and rescoped to a bounded recent-activity window (`RECENT_SCAN_BLOCKS = 500`, `EVENT_QUERY_CHUNK = 9` to respect the 10-block cap) — used only to merge in a brand-new mint/campaign the local table doesn't know about yet, self-healing that row into the table on the way. A scan failure or an incomplete result can no longer make an already-known asset disappear from `/api/onchain/list` or `/api/escrow/campaigns`.
- **`GET /api/portfolio/:wallet` rewritten to avoid `eth_getLogs` entirely**: known token ids now come from the local `onchain_tokens` table, then `chain.getBalance()` (`balanceOf`) and `chain.getPoolInfo()` (plain state reads, not subject to any log-range cap) fill in each holding's quantity, price, title, and artist. Verified directly against the live contract: `getBalance(1, <user's wallet>)` correctly returns `12`, and `getPoolInfo(1)`/`getPoolInfo(2)` return the real Guns/Black Sail pool state (`released: '12'`/`'0'`) — the RPC switch and query pattern both check out against real Sepolia data.
- **`server/.env`'s `CHAIN_RPC_URL`, `contracts/.env`'s `SEPOLIA_RPC_URL`** updated to the Alchemy endpoint. **Render's `CHAIN_RPC_URL` env var still needs the same update by the user** — not something this session can do directly.

**2.42 Unified catalogue and preproduction token release: both are now atomic, single-transaction on-chain releases (4 Sep 2026).** The user asked why buying a catalogue token and contributing to a preproduction campaign didn't work the same way — a catalogue `buy()` is one signed transaction that both pays and delivers tokens, but a preproduction `contribute()` only ever moved ETH into escrow; the matching tokens arrived via a *second*, backend-signed transaction (`releaseForContribution`, §2.34) that the frontend triggered automatically right after. Practically the tokens still showed up within seconds, but it wasn't the same on-chain guarantee, and the user explicitly wanted true parity rather than a faster-feeling version of the two-step design.
- **Contract changes**: `HumfiverseCatalogueToken` gained an `escrowContract` address (owner-settable via `setEscrowContract`) and `releaseFromPool` is now callable by the owner *or* that address, not owner-only. `HumfiverseMilestoneEscrow` takes the token's address in its constructor, `createCampaign` gained a `tokenId` parameter (requiring the token already be minted — `catalogueToken.totalSupplyOf(tokenId) > 0`), and `contribute()` now computes `qty = msg.value / pricePerToken` and calls `catalogueToken.releaseFromPool(msg.sender, tokenId, qty)` directly, inside the same transaction, right after recording the contribution — the exact atomicity `buy()` already had. Same known, accepted limitation as before: a later cancellation/refund doesn't claw back tokens already released this way.
- `createCampaign` gaining a parameter tripped a "stack too deep" compiler error under the default codegen — fixed by turning on `viaIR: true` in `hardhat.config.js`'s solidity settings (compiles through Yul instead, no stack-depth limit there).
- **Redeployed both contracts to Sepolia** (bytecode change on both): `HumfiverseCatalogueToken` at [`0xfd8D1d02Df922B9839D05b5C24cA2C06eD9690dD`](https://sepolia.etherscan.io/address/0xfd8D1d02Df922B9839D05b5C24cA2C06eD9690dD#code), `HumfiverseMilestoneEscrow` at [`0xa1670bC06e2d2860e8F7c1d80e6c13a501bcaF81`](https://sepolia.etherscan.io/address/0xa1670bC06e2d2860e8F7c1d80e6c13a501bcaF81#code), both verified on Etherscan. `scripts/deployEscrow.js` now takes `CATALOGUE_TOKEN_ADDRESS`, deploys the escrow with it, and calls `setEscrowContract` in the same script — deploy order is always token first, then escrow.
- **Re-created the two real assets on the new contracts** (a direct `ethers` script, not through the backend, since the backend's mint/campaign endpoints refuse to re-mint an assetId with an existing local-table row): re-minted Guns (id 1, supply 1300) and Black Sail (id 2, supply 1500) at their original price/title/artist, re-registered the "Peppe o bio" studio, and recreated Guns' escrow campaign with its original funding goal and milestone structure. **Manually re-released the user's 40 real Guns tokens** (their actual balance on the old contract at redeploy time, confirmed via `balanceOf`, not the earlier session's now-stale 12) to their wallet on the new contract, so their real holdings weren't lost — same "make the wallet whole after a redeploy" pattern as §2.36's migration, done carefully this time by reading the exact current balance first rather than reconstructing it from partial history.
- **New admin endpoints** (`ADMIN_API_KEY`-gated, same pattern as `DELETE /api/assets/:id`): `DELETE /api/admin/onchain-reset/:assetId` clears an asset's local `onchain_tokens`/`escrow_campaigns` cache rows so it can be re-minted against a new contract deploy (without this, `mintAssetOnchain`/`createEscrowCampaign` both correctly refuse to re-mint an assetId that already has a row — right for normal operation, wrong immediately after a redeploy). `DELETE /api/admin/escrow-studios-reset` clears the whole `escrow_studios` cache, since a studio's cached id becomes meaningless the moment the escrow contract it was registered on is replaced. Both are genuinely reusable — this is the third real contract redeploy this project has done.
- **`releaseForContribution` and `POST /api/onchain/release-for-contribution` removed entirely** (backend and frontend) — now dead code once `contribute()` handles release atomically, and dangerous to leave in place: calling it after an already-atomic release would have double-issued tokens. `onchain_releases`, the table that backed its idempotency check, was dropped too.
- **Fixed a real race condition while wiring this up**: the onboarding wizard's mint call and escrow-campaign-creation call used to fire in parallel (mint not awaited before the escrow call went out) — harmless before, since the two contracts never depended on each other, but `createCampaign` now requires the token to already exist on-chain, so the race could make campaign creation revert if it reached the chain first. Fixed by awaiting the mint and only proceeding to escrow creation on success, passing the confirmed token id through.
- Verified end-to-end against the live redeployed contracts, not just the 68 passing Hardhat tests (2 new ones added, covering the atomic release and the `unknown token id` guard): minted both assets, recreated the campaign, made a real 0.005 ETH `contribute()` call, and confirmed the balance moved 40→45 **in that same transaction** — no second call, no delay.

**2.43 Real audio-file upload, linked on-chain to the token (6 Sep 2026).** Explicit user request: a system to upload the actual track and link it, on-chain, to the contract that mints/sells its tokens. Before this, the onboarding wizard never touched a real audio file at all — only metadata (title, artist, genre) and a mock "statement" field that just stored a filename string, never an upload.
- **Storage decision, given to the user as a real tradeoff** (Render's free-tier disk is ephemeral, wiped on every redeploy — not viable for user-uploaded files): IPFS via a free Pinata pinning account, chosen over centralized cloud storage or a URL-only field, matching the project's "on-chain, not just off-chain-and-trusted" philosophy elsewhere (§2.24's trackTitle/artistName precedent). The user created the account and provided the API key directly, same flow as the Alchemy RPC key earlier.
- **Contract**: `HumfiverseCatalogueToken` gained `trackAudioUri` (per-token id, empty string = unlinked) and owner-only `setTrackAudioUri` — deliberately a separate setter, not a `mintCatalogue` parameter, since the IPFS upload and the mint are two independent operations that can each fail on their own. Surfaced in the ERC-1155 metadata JSON's `animation_url` field (the standard wallets/marketplaces read for audio/video) as a convenience mirror — the on-chain value stays the source of truth, same pattern as trackTitle/artistName.
- **A real cost of the §2.42 design surfaced here**: `HumfiverseMilestoneEscrow`'s reference to the token contract is `immutable`, so adding this field to the token meant redeploying *both* contracts again, not just the token — flagged to the user as an explicit tradeoff before starting (asked: skip on-chain storage entirely and use only the off-chain metadata JSON, or accept another full redeploy cycle) and confirmed to proceed with a full redeploy.
- **Backend**: new `server/pinata.js` — no new npm dependency, uses Node's built-in `fetch`/`FormData`/`Blob` (stable since Node 18) to call Pinata's `pinFileToIPFS` REST endpoint directly. New `POST /api/onchain/audio/:assetId?filename=...` reads the raw file bytes from the request body (a new `readRawBody`, 20MB cap — the existing `readBody` is JSON-only and capped at 1MB, far too small for audio), uploads to Pinata, then calls the new `chain.setTrackAudioUriOnchain`.
- **Frontend**: the onboarding wizard's source step gained a real file input (`accept="audio/*"`, 20MB client-side check matching the backend's), applying to both catalogue and preproduction campaigns. Uploads after the mint succeeds (reusing the awaited-mint fix from §2.42), best-effort — a failed upload doesn't affect the token/campaign that already exist. Asset-detail's on-chain panel now renders an `<audio>` player when a track has one linked, resolving `ipfs://` through Pinata's public gateway for playback (the on-chain value itself stays protocol-neutral).
- **Fourth contract redeploy this project has needed** — same now-familiar playbook: `HumfiverseCatalogueToken` at [`0xd8820e0fb8F6229577BcdfA0BaAF864280B969a4`](https://sepolia.etherscan.io/address/0xd8820e0fb8F6229577BcdfA0BaAF864280B969a4#code), `HumfiverseMilestoneEscrow` at [`0x170c825f68024D0b919BfacecD0D8FcFDc639f8d`](https://sepolia.etherscan.io/address/0x170c825f68024D0b919BfacecD0D8FcFDc639f8d#code), both verified. Re-minted all three real assets (Guns, Black Sail, darios-516) and their escrow campaigns, and re-issued real holders' actual balances read fresh right before the redeploy (Guns 45, Black Sail 9 — both had grown since §2.42's redeploy from further real testing; darios-516 stayed at 0) — not reused from stale numbers. Campaign `raised` counters were **not** replayed (would need real repeat `contribute()` calls for a cosmetic-only number with no downstream effect on a testnet demo) — flagged here rather than silently simplified.
- `createCampaign` gaining a `tokenId` parameter in §2.42 had already required `viaIR: true` for a stack-too-deep error; unaffected by this round's changes.
- Verified end-to-end against the live redeployed contracts: uploaded a real file via `POST /api/onchain/audio/guns-448`, confirmed it landed on IPFS, confirmed `trackAudioUri` reads back correctly from the chain, and confirmed the token-metadata JSON's `animation_url` field picks it up.
- i18n shipped in all 9 locales (393/393 parity).

**2.44 Marketplace card audio preview, and the artist's wallet surfaced on the escrow panel (6 Sep 2026).** Follow-up to §2.43 the same day: the user wanted the uploaded track playable from the marketplace grid itself (a short preview), not only after opening the asset-detail page, and wanted the detail page's on-chain panel to show the funding data tied to the artist who registered the campaign, not just the campaign's own numbers.
- **Marketplace preview**: the decorative "play" icon already sitting on every card's cover art (`cover-play`, present since the original design but never wired to anything) is now functional whenever a track has a real `trackAudioUri` linked — clicking it toggles playback via a new `PreviewAudioService` (one shared `<audio>` element, so starting a preview on one card stops whatever was already playing on another) without navigating to the card's own link. Cards with no linked audio keep the exact old behavior — the icon stays purely decorative and clicks fall through to the card's link, unchanged. Extracted the `ipfs://` → Pinata-gateway conversion (previously only on asset-detail) into a shared `core/ipfs.util.ts` used by both.
- **Asset-detail escrow panel**: added the campaign's `artist` address (truncated, via the existing `WalletService.truncateAddr`) as a new stat tile — the panel already showed campaign id, raised/goal, and studio, but not who actually registered the campaign for funding, which is the piece the user specifically asked to see.
- Added a `pause` icon (`webapp/src/app/core/mock-data/icons.json`) — no icon set had one; every other icon in the app is either a static state or navigates away, this is the first inline play/pause toggle.
- i18n shipped in all 9 locales (394/394 parity).

**2.45 Backend split into layers: one 1,128-line `server.js` became a router plus four layers (10 Sep 2026).** No behaviour change — a pure restructure, done because the single-file backend had reached the point where three specific things were getting harder rather than merely being ugly. Documented in full in the new `server/STRUCTURE.md`.
- **The three problems this fixes, stated concretely.** (1) *Route precedence was positional*: `GET /api/onchain/list` only ever worked because its `if` block sat above `GET /api/onchain/:assetId` in the file, with nothing marking the dependency — moving a block would have silently broken it. (2) *SQL was scattered*: `onchain_tokens` alone was queried from five places with different column lists, so a schema change meant grepping the whole file. (3) *Nothing was reachable without a socket*: every route lived inside the `http.createServer` callback, so exercising one validation rule meant booting the server and issuing a real HTTP request.
- **The shape.** `routes/` → `services/` → `data/` → `db.js`, imports pointing one way only, with `lib/` and `config.js` as leaf modules outside the stack. A route never writes SQL; a service never sees `req`/`res` (it takes values, returns values, and throws errors carrying a `code` the route maps to a status); a repository never calls the chain and never validates. `chain.js`/`chainEscrow.js` are imported only by services, with one deliberate exception: routes call `mintingEnabled()`/`writeEnabled()` directly, since "is this deployment configured to sign transactions" is a 503 fact about the deployment, not a domain rule.
- **`lib/router.js` (~40 lines) replaces the `if`-chain**, and makes precedence structural instead of positional: literal paths are matched from a `Map` *before* any `:param` pattern, so `/api/onchain/list` can no longer lose to `/api/onchain/:assetId` regardless of registration order. Accepts a `RegExp` with named capture groups for the two ERC-1155 metadata routes, whose exact hex/digit shape is the point. Still no web framework — that was the only thing one would have provided here.
- **Two behaviour improvements that came for free, both replacing a hang with an answer.** Routes without their own `try/catch` (`GET /api/data`, the admin resets, and five others) used to throw into an un-awaited async handler: an unhandled rejection in the log and a request that hung until the client gave up. `app.js` now catches and answers `500`. Separately, `decodeURIComponent` on a malformed path param (a bare `%`) used to throw the same way; the router now falls back to the raw value, so a malformed id is an ordinary 404/no-op.
- **Verified by running both versions side by side**, old and new, on separate ports against separate copies of the same database, and diffing status code, `Content-Type` and body across **54 requests** covering all 27 endpoints plus precedence and negative cases (wrong method, wrong hex shape, wrong extension, unknown path, encoded slash in a param) — random receipt hashes and timestamps normalised. **54/54 identical, 0 differing.** Also confirmed every one of the 20 API paths `webapp/src/app/core/api.service.ts` calls still resolves.
- Left untouched: `db.js`, `chain.js`, `chainEscrow.js`, `chainRetry.js`, `pinata.js`, `contract-template.js`, `seed-data.js` — already single-purpose modules.

**2.46 Pulled shared logic out of the two largest components, and removed a duplicated milestone table that nothing kept in sync (11 Sep 2026).** Companion to §2.45 on the frontend side. `onboarding.component.ts` (482 lines) and `asset-detail.component.ts` (463) had accumulated pure logic that had nothing to do with either page's orchestration, and in one case the same facts written down twice.
- **The duplication worth naming, because it was a latent correctness bug rather than untidiness.** The four preproduction milestones were declared twice in `onboarding.component.ts`: once around line 299 as display amounts (`Math.round(total * 0.2)`, `* 0.4`, `* 0.3`, `* 0.1`) written onto `asset.milestones`, and again around line 431 as the basis points actually sent to `HumfiverseMilestoneEscrow` (`bps: 2000/4000/3000/1000`), with the four milestone names retyped in both places. Nothing checked that the two agreed. Editing one and forgetting the other would have shown the artist and every investor a tranche split the contract did not enforce — no error, no warning, the UI number and the on-chain number simply disagreeing. Now one `PREPRODUCTION_MILESTONES` table in `features/onboarding/campaign-draft.util.ts` is the source of both: the escrow payload *is* the table, and the display amount is derived from each entry's bps. Same for the optional catalogue-extra campaign's two tranches.
- **New shared utilities in `core/`**, all pure: `portfolio-holdings.util.ts` (`addHolding` — the "find the holding, add tokens and cost basis, otherwise push a new one" block that `applyPurchase` and `buyFromListing` each inlined separately), `royalty.util.ts` (`royaltyTotal`, `royaltyAvg`, and the `YYYY-MM` check that must match what the backend re-validates), and `onchain-error.util.ts` (`onchainErrorTranslation`, which decides *which* message a failed wallet call gets and returns a key plus params, leaving the component to do the translating).
- **New feature-local files**, since nothing outside the wizard has any use for them: `features/onboarding/onboarding.model.ts` (the `WizardData` shape, the step/genre/distributor/disclosure option lists, `freshWizardData`, the wallet-address check) and `campaign-draft.util.ts` (milestone templates, slug generation, `buildAssetDraft`, `buildCampaignDraft`, `canAdvanceFrom`). The contract step's validation deliberately stayed in the component: it needs the clause list off the loaded template, not just the draft.
- **One behaviour improvement, taken deliberately.** Both holdings upserts mutated the matched `Holding` in place and then spread the array around it — working only because the new array reference is what the signal notices. That is the same shape as the real bug `applyPurchase`'s own comment warns about for `assets`, left in place for `holdings`. `addHolding` rebuilds the row instead, and a test asserts it leaves its input untouched. No displayed number changes.
- **Verified by equivalence, not by inspection.** The extracted utilities were compiled to plain JS and run against the original inline code transcribed verbatim from git: **1,800 checks, 0 differing** — every asset and campaign draft across both models and a range of titles, budgets and histories; both escrow payloads; slug generation; every step-validation combination; the holdings upsert against three starting portfolios; every branch of the error mapping; and the royalty aggregates. Separately confirmed the basis-point tranche arithmetic matches the old fraction arithmetic for **every integer total from 0 to 200,000** (800,004 comparisons, 0 differing). `ng build` passes with Angular's full template type-checking, and the milestone-name literal now appears one fewer time in the built bundle, confirming the deduplication reached the output.
- Net: 482 → 368 and 463 → 427 lines, with the removed logic now readable, reusable, and testable on its own. **Not yet clicked through in a browser** — the equivalence proof and the build are what this rests on; a real pass over the wizard and the asset page is still worth doing.

**2.47 The onboarding wizard's two catalogue provenance fields now reach the asset record instead of being discarded (11 Sep 2026).** Closing the "cosmetic dead input" item flagged when the fabricated royalty-history generator was removed — which on inspection turned out to be two fields, not one.
- **What was actually happening.** The catalogue step collects a primary distributor/PRO (a `<select>` over Spotify for Artists, Apple Music for Artists, DistroKid, Believe, SIAE) and a "months of reported history" number. Both were written into the wizard's own draft, and the review step displayed the months figure back to the artist as part of the campaign summary — but neither was ever written onto the `Asset`, sent to the backend, or read by anything. The artist answered two questions and the answers were dropped when the wizard reset. The select's change handler was present and working; the values simply had no destination.
- **Why this mattered beyond tidiness.** These two fields are exactly the provenance of the self-reported royalty figures that replaced the fake generator. "Twelve months of history, reported by DistroKid" is the context that tells a reader what the reported numbers are and how much of what was claimed has actually been entered. Discarding it while asking for it is the same failure mode as showing a fabricated number: the interface implies a meaning the data does not have.
- **The change.** `Asset` gained `royaltySource` and `royaltyHistoryMonths`, both optional and catalogue-only. `buildAssetDraft` writes them for catalogue-kind campaigns (`royaltyHistoryMonths` only when the input parses to a positive integer, so an empty or junk value stays absent rather than becoming `0` or `NaN`). The asset page's royalty tab renders them on a new line in **both** branches — beside the reported history, and beside the "nothing reported yet" state, where the gap between what was claimed and what was entered is the more useful signal. Labelled as declared by the artist and not verified by Humfiverse, since nothing here checks either value.
- One new i18n key, `detail.royaltyDeclared`, shipped in all 9 locales (432/432 parity).
- Verified: every combination of distributor and months value produces the expected field or correctly omits it, and re-running §2.46's full equivalence suite with only these two fields added to the reference implementation still gives **1,800/1,800 identical** — the approved change is the only behavioural difference.

**2.48 Status of the items left open on 8 Sep, checked against live state rather than assumed (11 Sep 2026).**
- **Fake royalty history left on two campaigns — already gone.** All 7 assets in production return no `royaltyHistory` field at all, `primo-pezzo-783` and `secondo-pezzo-723` included. The admin cleanup endpoint did its job; nothing further to run.
- **Whether buying Guns succeeds on-chain — confirmed, with on-chain evidence.** `guns-448` now reads `poolBalance: 0` against `totalSupply: 1300`, and its escrow campaign reads `raised: 1.255 ETH` against a `1.3 ETH` goal. At 0.001 ETH per token that is exactly 1,255 tokens bought through `contribute()`, plus the 45 manually re-released across redeploys — the full 1,300. The remaining-tokens fix is what made that possible; before it the UI advertised 1,300 and a max-quantity buy reverted every time.
- **A consequence of that worth recording**: Guns now shows as sold out (`remainingFor` correctly prefers the real `poolBalance`, so the buy path is closed) while its funding bar read 96.53%. ~~Filed here as honest-but-odd rather than a defect.~~ **That reading was wrong — it was a real bug, corrected in §2.49 below.** The user rejected the explanation immediately and was right to: a page cannot say "1,300/1,300 tokens", "0 remaining" and "$13,000 of $13,000" beside a bar reading 96.53% and call it a deliberate design.
- **GitBook sync — working, and pointing at the wrong branch.** All 10 whitepaper chapters are published; the last import succeeded on 8 Sep. But Git Sync reads from **`dev/cesare`**, a branch `CLAUDE.md` explicitly describes as personal and expected to be force-pushed or rebased — so a rebase could alter the published whitepaper. It should track `main`, which already carries `whitepaper/` and both config files. **Not changed from here**: the GitBook API exposes no Git Sync configuration (the space lists zero integrations and the git-sync installation is not addressable through the integration endpoints), consistent with GitBook's own documentation that this wiring is UI-only. A one-time dashboard change.
- **The redundant root `.gitbook.yaml` — decided: keep it.** `gitbook-docs.yaml` is the file doing the work under the Site model, and `.gitbook.yaml` is at most inert. But the sync demonstrably works with both present, and verifying a removal means re-triggering a sync against the live published whitepaper. Not worth that risk for tidiness.
- **Self-reported royalties for catalogue onboarding — already built** (shipped as `fdf42e7`), and §2.47 above completes it with the provenance those figures were missing.

**2.49 The funding bar and the token counts beside it were measuring two different things (11 Sep 2026).** Reported by the user against Guns: every token sold, page still showing 96.53%. A real defect, and one this log had misfiled a day earlier as an acceptable quirk (corrected in §2.48 above).
- **The inconsistency, in one line.** `onchain-progress.util.ts` exports four functions that all answer "how far along is this campaign", and they drew on two different sources. `remainingFor` prefers the token contract's real `poolBalance`; `tokensSoldFor` and `fundingRaisedFor` are both derived from it, so all three are token-based. `fundingPctFor` was not — for a preproduction campaign it took the escrow's own `raised/fundingGoal` ratio first, which measures ETH that arrived through `contribute()` on the current contract.
- **Why they drift.** Tokens that leave the pool any other way never touch `escrow.raised` — above all the manual `releaseFromPool` re-issues this project runs after every contract redeploy to make real holders whole (§2.36/§2.42/§2.43). `remainingFor` was fixed to prefer the pool balance for exactly that reason; `fundingPctFor` was left behind, so the two diverged by precisely the number of manually released tokens. For Guns that is 45 of 1,300: once the other 1,255 genuinely sold, the asset page showed "1,300/1,300 tokens", "0 remaining" and a marketplace card reading "$13,000 of $13,000" — next to a bar at 96.53%. **Black Sail had the mirror image**: 9 tokens out and $90 raised, against a bar reading 0%, because none of those 9 came through `contribute()` either.
- **The fix.** `fundingPctFor` is now the percentage form of `tokensSoldFor()/a.tokensTotal` — the same two numbers the UI prints beside it — so the bar cannot contradict the tile or the USD figure. Truncation to two decimals matches the basis-point division it replaces, and the result is clamped to 0–100 because it drives a CSS width and a stale local record must not produce a negative or overflowing bar. The escrow ratio survives as the fallback for a preproduction campaign with no on-chain token data, and the mock counter below that; both are unchanged.
- **A second defect surfaced while testing the fix.** `format.util.ts`'s `fundingPct` divided by `a.tokensTotal` with no zero guard, returning `NaN`. Reachable: the wizard computes a preproduction campaign's supply as budget/10 and `canAdvanceFrom` does not require the budget to exceed zero, so a campaign created with every budget field emptied gets `tokensTotal: 0`. `width: NaN%` is silently ignored by the browser, so the symptom is a bar that never renders rather than anything anyone would notice. Guarded.
- **Verified** against all four live campaigns' real on-chain state, asserting the invariant that the percentage equals both `tokensSold/tokensTotal` and `raised/goal` in dollars, plus every fallback path and the two clamping edge cases. All pass; the fallbacks return exactly what they did before.
- Flagged here as still open — `campaign-card.component.ts` reading the mock counter — and fixed immediately after in §2.50.

**2.50 The other two surfaces still reading the mock counter, including a sold-out badge that could never appear (11 Sep 2026).** Direct follow-up to §2.49: having established that progress must come from the pool balance, the remaining places that did not read it were worth finding rather than waiting for each to be reported.
- **The artist's own dashboard was the worst of them.** `campaign-card.component.ts`, rendered on `artist-dashboard` and `for-artists`, computed its bar with the chain-unaware `fundingPct(a)`/`fundingRaised(a)` from `format.util.ts`, which read the mock `tokensSold` counter — never persisted anywhere, so it reverts to zero on every reload (§2.32). The effect on Guns: the marketplace card showed **100% and $13,000**, and the artist looking at their own fully-sold campaign on their own dashboard saw **0% and $0**. Now on the same chain-aware helpers as the marketplace card.
- **`status-chip.component.ts`'s sold-out branch was unreachable code.** It tested `asset.status === 'sold-out'`, and *nothing in the application ever assigns that value*: the onboarding wizard creates every asset as `'funding'` and no code path updates it. So a completely sold campaign kept advertising itself as open — Guns, pool balance zero, displayed "Preproduction" on all three surfaces that render the chip. It now derives sold-out from the real pool balance, keeping the stored flag as a second way in for data that sets it. Deliberately claims sold-out only when there *is* chain data to read: falling back to the mock counter would mark a campaign sold out the moment its token total is zero, which the wizard can still produce (§2.49).
- **`asset-detail` passes its own reading in** via a new optional `soldOut` input, because that page re-fetches both contracts right after a purchase and is therefore fresher than the store's boot-time maps. The two cards let the chip derive it, which is correct for them.
- **Two small consolidations while there.** `StoreService` gained `onchainFor(assetId)`/`escrowFor(assetId)`, since three components now need the same pair of map lookups and getting the `?? null` wrong silently changes which branch the progress helpers take. `asset-card.component.ts` adopted them in place of its inline duplicates.
- **Verified** by computing what each surface renders for all four live campaigns from their real on-chain state, and asserting the marketplace card and the dashboard card now agree on both percentage and dollars for every one. Plus seven cases over the chip rule, covering the override, the absence of chain data, and the zero-total campaign. §2.49's suite and §2.46's 1,800 equivalence checks both still pass.
- **Genuinely still open, and not fixable from the frontend**: `Campaign.holders` is written as `0` by the wizard and never updated, and the artist dashboard sums it into a "total holders" figure that is therefore always zero. A real count needs the set of addresses holding each token id, which no backend endpoint exposes — it would take event indexing or a per-wallet scan. Left showing zero rather than substituted with a plausible-looking number.

**2.51 The fixed funding bar still showed the old number while the chain read was in flight (11 Sep 2026).** The user reported 96.53% again after §2.49 and §2.50 were live on GitHub Pages. Verified first that the deployed bundle really did carry the fix — the live `main-*.js` is byte-identical to what `main` committed, and the old basis-point form is down from 4 occurrences to 2 across every live chunk — and that the API answers correctly from the Pages origin, CORS included. So the deploy was genuine, and there was a second defect.
- **`null` and `{ onchain: false }` were being treated as the same answer.** They are not. `null` means the read has not come back yet: the asset-detail page starts both signals at `null` and fills them from two independent promises, and the store's maps are empty until `hydrate()` finishes its whole batch. `{ onchain: false }` means the read came back and there is no token. Only the second is a case where the escrow's ETH ratio is the best available measure; the first is simply unknown.
- **What that looked like.** `refreshOnchainState` fires `getOnchainInfo` and `getEscrowCampaign` in parallel. Whenever the escrow response won the race, the page had escrow data and no pool data, took the escrow branch, and rendered the pre-fix figure: 96.53% and "1,255/1,300" on Guns, flipping to 100% and "1,300/1,300" when the pool balance arrived. Warm, that window is about 150ms. On a cold Render instance — `/api/onchain/list` was measured at 7.2s — it is seconds, which is long enough to read, and long enough to report as still broken.
- **The fix** gates both fallbacks on a known answer: `onchain && !onchain.onchain`. While the read is pending, both `remainingFor` and `fundingPctFor` fall through to the mock counter, so the page shows 0% and 0/1300 and then fills in, rather than showing a precise wrong number and correcting itself. Both had to change together — gating one and not the other would have reintroduced the §2.49 contradiction inside the loading window.
- **Verified** across all five load states: neither signal back, escrow back first, chain back first, both back, and chain-read-failed. 96.53% is now unreachable until the chain read has actually landed, and the bar agrees with the token tile in every state including the failure one. The three earlier suites still pass; two of their assertions were updated, because they encoded the behaviour this entry deliberately changes.

**2.52 A failed on-chain read was being recorded as "this asset has no token" — the actual cause of the 96.53% that survived three fixes (11 Sep 2026).** The user reported the number a fifth time and, decisively, pasted their page source: it loaded `main-LEWVCAPR.js`, the current build. That ruled out cache, ruled out a stale deploy, and meant the remaining fault was real.
- **The reasoning that found it.** 96.53% carries two decimals, and the mock fallback rounds to whole numbers, so the figure can only come from the escrow's `raised/fundingGoal` ratio. After §2.51 that branch is reachable only when `onchain` is `{ onchain: false }`. Production returns `onchain: true` for Guns. So something in the client had to be *manufacturing* `{ onchain: false }` — and it was: both `asset-detail.component.ts` and `store.service.ts` mapped a **failed request** to exactly that value.
- **Why that is wrong.** `{ onchain: false }` is a claim: this asset has no token on chain. A request that timed out is not evidence of that; it is evidence of nothing. Conflating the two handed the funding helpers the one input that unlocks the escrow ratio, and because nothing retried, the wrong number stayed for the life of the page. This is the same null-versus-false confusion as §2.51, one layer further out — that entry fixed the *pending* case and left the *failed* case conflated.
- **Why it hit this user and not the curl checks.** The backend is on Render's free tier and sleeps after 15 minutes idle; a cold `/api/onchain/:assetId` also waits on a free-tier RPC. Measured in this session: `/api/onchain/list` at 7.2s warm, and a chain-scan path at 48.9s cold. A request that slow can fail in a browser while every check from a terminal, run against an already-warm instance, passes.
- **The fix, in two parts.** A failed read now leaves the state unknown — `null` on the detail page, and simply absent from the store's map — so the helpers fall through to the plain counter instead of a precise wrong number. And both on-chain reads now go through a small bounded retry (`core/retry.util.ts`, three attempts with backoff), so a cold instance self-heals into the correct figure rather than leaving the page permanently unable to say how much of a campaign sold.
- **Verified**: for Guns, 96.53% is now unreachable in every outcome the client can produce — read pending, read failed, read succeeded. It remains reachable only if the *server itself* reports no token, which is the one case where the escrow ratio is the honest answer. The retry helper is covered for recovery, for not making extra calls on success, and for giving up and rethrowing rather than hanging. All three earlier suites still pass.
- **Process note worth keeping.** Three fixes shipped against this symptom before the real cause was found, and each was a genuine defect. What ended it was the user pasting their page source: that single fact eliminated every environmental explanation at once and forced the search back into the code. Asking for the loaded bundle filename should come *first* next time, not fifth.

**2.53 Drove the live site in a real browser, which settled the funding-bar question and found one more mock-counter reader (11 Sep 2026).** After the fifth report, stopped reasoning about what the code *should* render and drove headless Chrome over the DevTools protocol against the published site, capturing the loaded bundle, every `/api` call, and the rendered text.
- **The campaign page is correct and has been verified as such.** Five consecutive renders of `#/asset/guns-448` on GitHub Pages: bundle `main-RFVOKCB2.js`, every API call 200, no failed requests, and the page showing **"TOKENS 1,300/1,300", "FUNDED 100%"** and a **"Sold out"** chip. 96.53% did not appear once. Worth keeping as the method: a CDP session is about forty lines with no dependencies, and it answers "what does the user actually see" in a way no amount of reading the source can.
- **It did find a real defect on the artist dashboard.** `artist-dashboard.component.ts` summed the chain-unaware `fundingRaised()` from `format.util.ts` for its "total raised" tile — the mock `tokensSold` counter that is never persisted. So an artist whose campaign had genuinely sold out saw **"TOTAL RAISED $0"** on their own dashboard, right above a card that §2.50 had already fixed to read the real pool. Missed in that pass because the grep there looked for the chain-aware helper names and this file used the mock one. Now on `fundingRaisedFor` with the same two store maps. That leaves no consumer of the mock helpers anywhere except `onchain-progress.util.ts` itself, where it is the documented last-resort fallback.
- **The latency problem it exposed turned out to be the dashboard's real bug**, fixed in §2.54 below.

**2.54 One slow endpoint was gating every funding figure in the app (11 Sep 2026).** The artist dashboard still showed "TOTAL RAISED $0" and every campaign at 0% after ninety seconds, with the campaign page beside it correctly showing 100%. Driving it under CDP showed all the per-asset on-chain reads completing — and the numbers never updating.
- **The cause.** `hydrateFromBackend()` ran its loads in a chain, then joined the last two with a single `Promise.all` and set **both** maps only once **both** had resolved. So the slowest call gated every card in the application. Measured against the live backend: `GET /api/escrow/campaigns` took **200s cold and 31s warm**, because it runs a chain scan plus a contract read per campaign. For that entire window the pool data was already fetched and sitting unused, while every card fell back to the plain counter and showed a confident zero. The per-asset reads were themselves waiting on `GET /api/onchain/list`, a second chain scan, for asset ids `GET /api/data` had already returned.
- **The fix.** Three independent loads, each publishing its own signal the moment it lands: the listing check that gates the marketplace, the per-asset pool reads (now started straight from the ids already in hand), and the escrow batch. Nothing waits on anything else. The dashboard's numbers now appear as soon as the pool reads return, instead of being held behind a call that can take three minutes.
- Worth noting what this was *not*: every individual request was returning 200 the whole time. Nothing failed, nothing errored, and no log would have shown a problem. The defect was purely in what the client waited for before believing any of it.

**2.55 The chain scan on the read path: 50 sequential RPC round trips, paid on every request, to look for something almost never there (11 Sep 2026).** Follow-up to §2.54, which found `GET /api/escrow/campaigns` taking 200s cold and 31s warm and blocking every funding figure in the app. This is the backend half.
- **Where the time went, measured rather than guessed.** Timing the pieces directly against the live RPC: the recent-blocks scan took **3.4s**, reading *all five* campaigns in parallel took **0.35s**. The scan walks its 500-block window in 10-block steps because that is the free-tier `eth_getLogs` cap, so it is **50 sequential round trips**, each wrapped in a retry. On a slower or rate-limited RPC that is where the 200s came from. `GET /api/onchain/list` carried an identical scan.
- **What the scan was for, and why it was the wrong place.** §2.39 established the local tables as the primary source with a bounded scan merged in to catch a record not cached yet. But a campaign or a mint is only ever created *through this backend*, which writes its row in the same call — so the scan was paying 50 round trips per request to look for something that is essentially never there. Worse, its window covers about 500 blocks, roughly 1.7 hours of Sepolia: verified against an emptied cache, it recovers **0** of this project's real campaigns, all of which are older than that. It was buying almost nothing at a very high price.
- **The change.** Both listings run the scan only when their table is empty, which is the signature of the cache loss it exists to recover from. Per-asset self-healing is untouched: `findTokenWithChainFallback` still scans on a single-asset miss and re-seeds the row, and the frontend requests every asset individually, so a lost row still heals — just not on the listing path. The per-campaign lookup stays chain-native on purpose (§2.18), since asking the contract for the id costs one call and cannot return another campaign's data the way a cached id can after a redeploy renumbers them.
- **Measured, old against new, same seeded database, output compared:**

| call | before | after | output |
|---|---|---|---|
| `listCampaigns` | 3,812 ms | 299 ms | byte-identical, 5 campaigns |
| `listMintedAssetIds` | 7,457 ms | 1 ms | same asset ids |

- Over HTTP against a database seeded to match production: `/api/onchain/list` 2ms, `/api/escrow/campaigns` 0.6s. The empty-table recovery path was verified to still run the scan.

**2.56 One request for every card, and a placeholder instead of a zero the page is about to replace (11 Sep 2026).** The two follow-ups §2.55 left open, done together because they address the same experience from opposite ends: how long the cards show nothing useful, and what they show while they do.
- **`GET /api/onchain/batch?ids=…`** returns every asset's chain state in one call, keyed by asset id. The frontend was making **one request per asset** — seven on a page with seven campaigns, each doing its own chain read against a single free-tier instance. `getTokenView`/`getTokenViews` in `onchain.service.js` build the response from one shared shape, so the batch and the single-asset route cannot drift in what they report; verified field-for-field against `GET /api/onchain/:assetId` for three assets. The batch deliberately reads the local table directly rather than through `findTokenWithChainFallback`, because that fallback scans the chain on a miss (fifty sequential round trips, §2.55) and doing it once per missing asset would put the very cost this endpoint removes straight back in. A missing asset is omitted, which the client reads as unknown; the single-asset route keeps the fallback and the client requests any asset it opens, so a lost row still heals.
- **The cards no longer print a confident zero while they wait.** `StoreService` exposes `onchainInfoLoading`, and both cards plus the dashboard's total render an em dash and an empty meter until the pool read lands. This is the small lesson of the whole day: almost every wrong number reported in this session was a *fallback* value displayed with exactly the same authority as a real one — "$0", "0%", "96.53%" — with nothing on screen to say the app was still guessing. A placeholder is not cosmetic here; it is the difference between "not known yet" and "known to be zero".
- **Verified in a real browser, not by reading the code.** Against a database seeded to match production: one `batch` request in the network log instead of seven, dashboard settled at **1.2s**, and with the batch endpoint deliberately delayed six seconds behind a proxy, the dashboard showed **"—"** for the total and both card figures while it waited, then **"$13k of $13k, 100%, Sold out"** once it landed.

**2.57 Resale listings were never stored anywhere, and could only be created from one page (12 Sep 2026).** Reported by the user: the tokens they had put up for sale on Guns were gone from the campaign page, and offering tokens was only possible from Portfolio.
- **Why they vanished, and it was not a regression.** `secondaryListings` was a signal seeded from a bundled JSON file of three fictional offers. The Portfolio's sell flow appended to that signal in memory and nothing else — no request, no table, no row. A reload restored the three fictional rows and discarded whatever had been created. Nobody else ever saw a listing either. Exactly the failure campaign records had before §2.20, in a feature that never got the same treatment.
- **Now persisted and shared.** New `secondary_listings` table, `data/listings.repo.js`, `services/listings.service.js` and `routes/listings.routes.js`, following the layering from §2.45. `GET /api/listings`, `POST /api/listings`, `POST /api/listings/:id/cancel`, `POST /api/listings/:id/buy`. Cancelling is gated on the listing's own seller — not authentication, which this backend has none of, but enough that one wallet cannot delete another's offer by guessing an id.
- **The one thing that is genuinely verified.** Creating a listing reads the seller's real balance off the token contract and refuses an offer larger than they hold. That turns a listing from an unverifiable claim into a verified one, which is the most it can honestly be: `HumfiverseMarketplace.sol` is written and tested but **has never been deployed**, so nothing escrows the tokens or settles a trade. The balance is checked once, at creation; nothing stops the seller moving the tokens afterwards, and the sell dialog says all of this in plain words rather than implying a locked, guaranteed offer.
- **The seller is now a wallet address.** It used to be the literal string `'you'`, which was fine while a listing lived in one tab and would have read as "you" for every visitor once shared. Rows store the lowercased address; the UI renders "you" when it matches the connected wallet and a truncated address otherwise.
- **Offering tokens moved to where the tokens are.** The campaign page's resale card gained the seller's own holding for that asset, read from the same endpoint Portfolio uses, a *list for sale* button, a cancel button on your own rows, and the same quantity/price dialog. Portfolio keeps its version; both now write through the API. Eight new i18n keys across all 9 locales (440/440), and the empty-state text no longer tells you to go to the Portfolio to do something you can now do in front of you.
- **Verified**: every endpoint exercised directly, including the balance refusal (`409`, "wallet holds 1300 token(s), cannot list 999999"), cancel-by-wrong-wallet (`403`), unknown id (`404`), and **survival across a server restart** — the actual bug. Then in a real browser against a seeded database: the campaign page showed "Resale marketplace", "From $14", the listing row with a truncated seller, and the new panel correctly reading "Connect your wallet to offer tokens for resale."
- **The honest next step**, unchanged by any of this: a real resale needs `HumfiverseMarketplace.sol` deployed and the tokens actually escrowed. Until then this is an offer board with a verified balance, and is labelled as one.

**2.58 The resale listings shipped in §2.57 let anyone list or cancel on any wallet's behalf (12 Sep 2026).** The user caught this immediately and correctly: *"how is it possible the site lets me list tokens for sale without asking for a wallet signature?"* They were right, and the answer is that it should not have.
- **The hole, demonstrated rather than asserted.** `seller` was just a string in the request body, with nothing tying it to the caller. Against a local instance: a caller holding nothing published an offer of **500 of another wallet's tokens at $0.01**, and then **cancelled that wallet's own listing**. The "only the seller can cancel" check in §2.57 was worthless, because `GET /api/listings` returns the seller address — there was nothing to guess. The balance check never helped: it verifies the *named* wallet can deliver, not that the *caller* is that wallet.
- **The fix, now live.** The seller signs a plain-text message stating the exact terms — asset, quantity, price, wallet, timestamp — and the server rebuilds that message from the submitted fields and recovers the signer with `ethers.verifyMessage`. A signature therefore authorises only the precise offer it was shown: change the quantity, price, asset or wallet and recovery no longer matches. Signatures expire after ten minutes. Cancelling is signed too. Verified against every path: no signature, garbage signature, a signature obtained for a different quantity, a signature from a different wallet claiming to be the holder, and a thirty-minute-old signature — **all refused with 401**; the properly signed request succeeds.
- **`lib/listing-signature.js` and `core/listing-signature.util.ts` must stay byte-identical**, since one builds the message the wallet signs and the other rebuilds it to verify. A single character of drift breaks every signature, so they are covered by a test that compares their output directly and then signs with the frontend's text and verifies with the backend's.
- **What this is and is not.** It is authentication: proof of who asked. It is **not** settlement — it moves and locks nothing, and the message says so in plain words so nobody signs it believing their tokens are now committed.
- **The user's own proposal is the right destination, and the contract already implements it.** `HumfiverseMarketplace.sol`'s `list()` is called by the seller, so `msg.sender` *is* the authentication, and it additionally requires `isApprovedForAll` — meaning the contract can actually move the tokens when a buyer arrives, which a signed message cannot. One nuance worth stating: the contract is deliberately **non-custodial**. The tokens do not move into it at listing time; the seller keeps them, and they transfer atomically at purchase. That is the standard marketplace pattern and is better for the seller than escrow.
- **What deploying it would take**, checked rather than guessed: the marketplace takes only a fee-recipient in its constructor and reaches the token through `IERC1155`, so **no redeploy of the token or the escrow is needed** — unlike every previous contract change this project has made. It needs a deploy script, the `list`/`cancel`/`buy` calls wired through `WalletService` (which already signs four other transaction types), and the backend demoted from source of truth to an index of on-chain listings. 18 Hardhat tests already cover the contract.

**2.59 Resale moved onto the chain: listing is now the seller's own transaction (12 Sep 2026).** The user's objection in §2.58 — *"I should authorise this myself with a signature, after which the tokens go into the sale contract"* — was right about the principle, and `HumfiverseMarketplace.sol` had implemented it since it was written. It had simply never been deployed.
- **Why the signature fix was not enough.** It proved *who asked*. It could not make an offer binding: nothing could compel a seller to hand over tokens when a buyer arrived. `list()` takes `msg.sender` as the seller, so the transaction itself *is* the authentication, and it additionally requires `isApprovedForAll` — which means the contract can actually move the tokens at purchase time. That is the difference between a claim and an offer.
- **One correction to the user's model, worth stating.** The contract is deliberately **non-custodial**: tokens do **not** move into it at listing time. The seller keeps them and can still transfer them; they move to the buyer atomically inside `buyListing`, along with the ETH to the seller and the 1% token fee to the platform. This is the standard marketplace pattern and is strictly better for the seller than escrow — at the cost that a listing can outlive the balance behind it, which the board now shows as a "seller no longer holds these tokens" chip rather than letting a buyer discover it at signing time.
- **No redeploy of anything else.** The marketplace takes only a fee recipient in its constructor and reaches the token through `IERC1155`, so `HumfiverseCatalogueToken` and `HumfiverseMilestoneEscrow` are untouched and no holder balance or campaign moved. **The first contract change in this project's history that did not cascade.** Nothing on the token side authorises the marketplace either; each seller approves it themselves, which is exactly why Humfiverse cannot move anyone's tokens.
- **The backend is demoted to an index.** `marketplace_listings` stores only which listing ids exist — quantity, price, seller and whether a listing is still open are read off the contract on every request, so a cancellation or sale made directly on chain is reflected without this server being told. `POST /api/listings/index` needs no authentication, and that is a property of the design rather than an oversight: the id is verified against the contract, so a claim that isn't true there never enters the index. The three write endpoints from §2.57 now answer **410**, the same way `/api/escrow/confirm` does (§2.27), because they were removed on purpose and a caller deserves to know which.
- **The signed-message layer added in §2.58 is gone**, front and back. It was the right fix for one hour and is redundant against a transaction the seller signs themselves; keeping two authentication mechanisms for one action would be worse than either.
- **The bundled sample listings are gone too.** A board where every other row is a real, signed, on-chain offer is no place for three fictional ones, so it starts empty and says "resale unavailable" when no marketplace address is configured rather than implying nobody is selling.
- **Deployed** (by the user, since this environment's permission classifier blocks the deploy command): `HumfiverseMarketplace` at [`0x88af7374622cb99C015C2435202d3f1392264356`](https://sepolia.etherscan.io/address/0x88af7374622cb99C015C2435202d3f1392264356), block 11688640, owner and fee recipient both the deployer, `PLATFORM_FEE_BPS` 100. **No deploy block is recorded anywhere**, unlike the other two contracts: this backend reads listings by id straight from the contract and never scans events, so there is no starting block to remember (§2.55).
- **Verified end to end against the live contract**, not just compiled: approved the marketplace, listed 5 Guns tokens at $15, indexed the id, and read it back through `GET /api/listings` as `5 tokens, 1500000000000000 wei, deliverable: true`. Indexing refused both a wrong asset for that id and an id that does not exist (404 each). Then **cancelled the listing directly on the contract without telling the backend** — the board dropped it on the next read and pruned the stale id from the index by itself, which is the property the whole index-not-ledger design exists for.
- Set `CHAIN_MARKETPLACE_ADDRESS` on Render before merging, or the board reports itself unavailable until you do.

**2.60 The buy button would complete a purchase with no wallet, no signature and no transaction (12 Sep 2026).** Reported by the user: *"when I buy tokens it doesn't ask for a signature… and the transfer gives me no hash on the blockchain, so I have to conclude nothing real happens on chain."* Correct on both counts.
- **The path.** `buy()` tried three real routes in order — contribute to an active escrow, contribute to a catalogue's extra campaign, buy from the token pool — and **every one of them required a connected wallet**. With none, all three were skipped and control fell through to the last two lines of the method: `applyPurchase()` followed by `success.set({ qty, total })`. That updated local state and opened the same "Purchase confirmed" dialog the real purchase opens. The only difference was the missing transaction-hash block, which is absent rather than contradicted — so the dialog reads as a completed purchase either way.
- **Why the button was reachable at all.** It was gated on the acknowledgement checkbox alone: `[disabled]="!ack() || onchainBuyPending()"`. The panel above it sits behind investor verification, but that check is itself simulated and needs no wallet, so completing it is enough to reach a buy button with nothing connected.
- **The fix.** A `buyBlockedReason()` gate on both the click handler and the button's disabled state, with three outcomes. `'wallet'` when a real on-chain path exists and no wallet is connected — the button says so instead of buying. `'loading'` while `onchainInfo` is still `null`, because until the read lands this app does not know whether a real path exists, and acting on that unknown is precisely how it invented one — the same `null`-versus-`false` distinction that caused §2.51 and §2.52. And `null` when there genuinely is no on-chain token, which is bundled demo data or an unreachable backend; that branch still simulates, but now sets `simulated: true` and the dialog carries an explicit warning that nothing was recorded on chain and no tokens changed hands.
- Verified across all eight combinations of asset kind, chain state, escrow state and wallet presence: the only route that still reaches a simulated purchase is an asset the chain has positively reported as having no token.
- **The pattern this session keeps finding.** Every defect today has been the same shape: a fallback value or a fallback path presented with exactly the authority of the real one. A 96.53% that was an escrow ratio standing in for a pool balance; a `$0` that meant "not loaded"; a `{ onchain: false }` that meant "the request failed"; and now a purchase dialog that meant "we pretended". The fix each time was not new logic but refusing to let the substitute look like the original.

**2.61 Listing a resale failed on any price with cents, and silently mispriced the rest (12 Sep 2026).** Reported as *"it tells me the listing could not be updated"* — a message that, being generic, was itself part of the problem.
- **The cause, and it was introduced in §2.59.** The resale dialog invites a price in cents (`min="0.01" step="0.01"`), but the on-chain conversion reused `usdToWei`, which was written for mint-time prices and rounds to whole dollars. So $15.50 listed at **$16** without saying so, and anything under $0.50 converted to **zero wei**, which `HumfiverseMarketplace` rejects outright: `require(pricePerToken > 0)`. Reusing a whole-dollar helper for a free-form price field was the mistake.
- **Fixed with `usdToWeiPrecise`/`weiToUsdPrecise`.** A cent is 1e12 wei at this project's mapping — an exact integer — so nothing needs to round at all. The original pair is untouched and still used for mint prices and escrow contributions, which really are whole-dollar amounts. Verified against the **live contract**: $0.25 (which the old conversion turned into 0 wei and the contract refused) and $15.50 (which it turned into $16) both now list, read back off the contract at exactly the price typed, and were cancelled afterwards.
- **The second half of the bug was the message.** The portfolio's catch blocks showed a single `portfolio.listingFailed` for every failure, so a declined signature, a wrong network, insufficient gas and a contract revert all looked identical — and the contract's own `"price must be > 0"` never reached the screen. The campaign page had already been given `onchainErrorTranslation` for exactly this reason (§2.49's sibling problem); the portfolio now uses it too. **This is the fourth time in this session that a generic fallback message has hidden the real cause**, and it is worth treating as a rule: a catch block that cannot distinguish causes is a bug in its own right, not a tidy default.

**2.62 Guns' last milestone could never be released, and the error said nothing (12 Sep 2026).** The user confirmed milestones as studio and kept getting "could not complete the milestone". Both halves of that were real.
- **Artist and studio are the same wallet on this campaign**, which the asset page already warns about, and the contract permits: `confirmMilestoneAsStudio` only checks `msg.sender == studios[c.studioId].wallet`. So confirming both sides from one wallet is allowed, and milestones 0–2 did confirm and release (1.17 ETH paid out).
- **Milestone 3 reverted every time, and the revert rolled back the confirmation with it.** Its tranche is 0.13 ETH; after the first three paid out, the contract held 0.085. `_tryRelease` gates each milestone on `c.raised >= amount` — the *total raised*, not the balance still available — so all four passed their check while the money ran out at the fourth. Since the release happens inside the confirming transaction, `require(sent, "payout failed")` reverted the whole call, including `studioConfirmed = true`. The state therefore looked untouched after each attempt, which made it read as a permissions problem rather than an empty till.
- **The root cause is the 45-token gap again** (§2.49): the campaign's goal is 1.3 ETH and its tranches sum to exactly that, but only 1.255 ETH was ever contributed, because 45 tokens were re-released by hand across past redeploys without ETH entering the escrow. The same discrepancy that produced the 96.53% funding bar also made the final tranche unpayable — a second consequence nobody had noticed.
- **Closing it needed 46 transactions, not one.** `contribute()` also releases tokens (`qty = msg.value / pricePerToken`), and Guns' pool is empty with `releasedOf == totalSupply == 1300`, so any release reverts with "exceeds supply". Only contributions small enough for `qty` to round to zero get through, since that call is guarded by `if (qty > 0)` — strictly under the 0.001 ETH token price. A one-off resumable script sent 46 contributions of ~0.00098 ETH; raised is now exactly 1.3/1.3 and the contract holds exactly the 0.13 ETH the last tranche needs. Script deleted after running, per this repo's convention for one-off migrations.
- **And the error message was a bare `catch {}`** on both `studio.component.ts` and `artist-milestones.component.ts` — not even binding the error, so a declined signature, a wrong network, a nonce clash and a contract revert all showed one fixed string, and the contract's own reason never reached the screen. Both now use `onchainErrorTranslation`. **That makes six instances of this in one session** (see also §2.49, §2.61): a catch that cannot distinguish causes is a defect, not a default.

**2.63 "Your campaigns" listed everyone's campaigns, to everyone (12 Sep 2026).** Reported by the user: connected with a second account, the artist dashboard still showed Guns as theirs, along with Black Sail and darios, none of which that account created.
- **There was no filter at all.** The page iterated `store.campaigns()` — every campaign in the system — under a heading claiming ownership, and the three stat tiles above it (active campaigns, total raised, token holders) counted all of them too.
- **The deeper reason: the app had no notion of who owned a campaign.** `Asset` carried `artistName`, a display string, and nothing else. A name is not ownership, and nothing anywhere linked a campaign to a wallet — so there was nothing to filter *by*, which is presumably why it never was.
- **`Asset.artistWallet`**, recorded at creation from the connected wallet, plus `StoreService.campaignOwner()` which prefers it and falls back to the escrow contract's own `artist` address. That fallback matters: it attributes every existing campaign that has an escrow without any migration, because the chain already knew the answer this app had never stored.
- **Two campaigns remain genuinely unattributable** — `primo-pezzo-783` and `secondo-pezzo-723`, created before this and with no escrow to fall back on. There is no record anywhere of who made them, and inferring from the display name would be a guess dressed as a fact. They appear under no account, and the dashboard says how many exist rather than dropping them silently.
- **Verified against live production data**: `0x142F945e…` now sees 4 campaigns, `0x984067c9…` sees 1 (`sburricchio-646`, which is genuinely theirs), an unrelated wallet sees 0, and 2 are unattributable. Before the fix all three wallets saw all 7.
- With no wallet connected the page now asks for one instead of listing everything, which is also the honest answer to "which of these are mine".

## 3. Suggested phased build

1. **Phase 0 — Paper pilot**: one song or small catalogue, manual royalty verification, SPV set up, token issuance and distribution done as a scripted/manual process (not yet a polished product) to prove the legal-to-cash-to-token pipeline end to end with real royalty income, even a small amount. No governance module — if the pilot uses the pre-production variant, milestone escrow is still manually administered.
2. **Phase 1 — Minimum on-chain pilot**: smart contracts for token issuance and claims-based distribution on a testnet, then a single mainnet L2 deployment for the same pilot catalogue, with the royalty-confirmation oracle step still manual/attested. If the pre-production variant is in scope, the milestone-escrow contract ships here — still no governance/voting.
3. **Phase 2 — Marketplace**: multi-catalogue listing, fiat on-ramp, KYC integration, basic secondary transfer (compliant with whichever legal path was chosen). **This is where the advisory governance module (§2.7) ships**, once the SPV-manager role, shortlist-vetting process, and vote-weight caps are properly in place — not before.
4. **Phase 3 — Scale**: decentralize/professionalize the oracle step, consider secondary-market liquidity mechanisms (AMM or order book, subject to what's legally permissible for the token type chosen, and kept architecturally separate from the revenue-distribution vault per §2.3).

## 4. Open technical questions to resolve with the business/legal decisions

- Does the token need on-chain transfer restrictions (security path) or not (collectible path)? This changes the token standard choice directly.
- Will royalty administration be built in-house or outsourced to an existing royalty/PRO administration service in the pilot phase? Outsourcing is almost certainly faster for a first pilot.
- What's the settlement currency for distributions — stablecoin, or fiat off-ramp? Stablecoin is simpler on-chain but adds a conversion step for most retail users; this is worth user-testing rather than assuming.

---
*This is an architecture proposal for planning purposes, not a build-ready spec. Chain and vendor choices are illustrative starting points based on what comparable platforms use, not final recommendations — worth validating against the legal path chosen before writing code.*
